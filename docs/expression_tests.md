# Expression Test Coverage Map

This document maps the current expression tests to the checklist in
`docs/expression_cov.md`.

It is a snapshot of what exists today, not a future plan. Historical numbering
gaps from the checklist are preserved here as-is.

## Legend

- `covered`: there is a direct test for the checklist point, or a very close
  equivalent that exercises the intended contract surface.
- `partial`: nearby behavior is tested, but the full matrix, negative space, or
  a key variant is still missing.
- `none`: no current test directly covers the point.
- `infra`: harness or implementation guardrail only; useful, but not a direct
  checklist-coverage point.
- `done`: the row has been checked and its current state is accepted.

## Test Inventory

Manifest-backed suites:

- `tests/expression_parse.rs`
  - runners: `parse_positive_manifest_parses`,
    `parse_negative_manifest_matches_snapshots`
  - code-only: `parse_no_panic_corpus_holds`
  - manifests: `PP` = `parse_positive_manifest.json` (16 cases), `PN` =
    `parse_negative_manifest.json` (12 cases)
- `tests/expression_event_runtime.rs`
  - runners: `event_runtime_positive_manifest_matches`,
    `event_runtime_negative_manifest_matches_snapshots`
  - code-only: `event_runtime_short_circuit_holds`,
    `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface`
  - manifests: `ERP` = `event_runtime_positive_manifest.json` (32 cases), `ERN`
    = `event_runtime_negative_manifest.json` (10 cases)
- `tests/expression_integral_boolean.rs`
  - runners: `integral_boolean_positive_manifest_matches`,
    `integral_boolean_negative_manifest_matches_snapshots`
  - code-only: `integral_boolean_short_circuit_preservation_holds`
  - manifests: `IBP` = `integral_boolean_positive_manifest.json` (265 cases),
    `IBN` = `integral_boolean_negative_manifest.json` (14 cases)
- `tests/expression_rich_types.rs`
  - runners: `rich_types_positive_manifest_matches`,
    `rich_types_negative_manifest_matches_snapshots`
  - manifests: `RTP` = `rich_types_positive_manifest.json` (86 cases), `RTN` =
    `rich_types_negative_manifest.json` (105 cases)

Rust unit / regression tests under `src/expr/`:

- `src/expr/parser.rs`: `typed_parser_rejects_unmatched_open_parenthesis`,
  `typed_parser_rejects_broken_union_segmentation`,
  `typed_parser_preserves_iff_binding_to_single_term`,
  `logical_parser_accepts_integral_boolean_surface_sample`,
  `logical_parser_accepts_rich_type_surface_sample`
- `src/expr/lexer.rs`: `lex_event_expr_tracks_keywords_and_spans`,
  `lex_logical_expr_accepts_integral_boolean_operators`,
  `lex_logical_expr_reserves_triggered_suffix`,
  `lex_logical_expr_accepts_real_and_string_literals`
- `src/expr/sema.rs`: `binder_rejects_unsized_concat_literal`,
  `binder_rejects_non_constant_replication_multiplier`,
  `binder_preserves_enum_identity_in_conditional_arms`
- `src/expr/eval.rs`: `logical_and_short_circuits_rhs`,
  `wildcard_equality_preserves_unknown_from_lhs`
- `src/expr/mod.rs`: `parse_event_expr_wrapper_supports_any_tracked`
- `src/expr/legacy.rs`: `event_expr_iff_binding_with_union`,
  `event_expr_iff_capture_parenthesized_logical_payload`,
  `event_expr_accepts_comma_union`

Harness / manifest-contract guardrails:

- `tests/expression_fixture_contract.rs`
  - `shared_positive_manifest_contract_accepts_tagged_cases_and_rejects_legacy_shapes`
  - `shared_negative_manifest_contract_enforces_host_context_and_runtime_timestamp`
  - `all_expression_manifests_deserialize_through_the_shared_contract`
  - `negative_manifest_snapshots_exist_and_no_expression_snapshots_are_orphaned`

Unless noted otherwise, manifest case IDs below are exercised through the suite
runner for the corresponding file.

## Checklist Matrix

### Event Expressions (`1`-`35`)

| ID | Checklist point | Status | Current tests | Notes |
| --- | --- | --- | --- | --- |
| 1 | `[1.1] Wildcard event surface form` | done | `PP:wildcard_any_tracked`, `ERP:wildcard_any_tracked_same_timestamp_dedup`, `RTP:wildcard_tracks_string_and_event_changes`, `parse_event_expr_wrapper_supports_any_tracked` | - |
| 2 | `[1.1] Named event surface form` | done | `PP:named_any_change`, `ERP:named_change_requires_previous_sample`, `RTP:named_string_event_matches_signal_change`, `RTP:named_event_operand_matches_raw_event_timestamp` | - |
| 3 | `[1.1] Edge event surface forms` | done | `PP:edge_keywords`, `ERP:edge_surface_form_posedge`, `ERP:edge_surface_form_negedge`, `ERP:edge_surface_form_edge` | - |
| 4 | `[1.1] Union surface forms` | done | `PP:edge_keywords`, `PP:comma_union`, `ERP:wildcard_any_tracked_same_timestamp_dedup`, `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface`, `event_expr_accepts_comma_union` | - |
| 5 | `[1.1] Gated event surface form` | done | `PP:iff_binds_to_preceding_term_only`, `PP:iff_parenthesized_payload`, `ERP:iff_bounded_ops_and_literals`, `RTP:rich_iff_with_triggered_real_and_string` | - |
| 6 | `[1.1] Wildcard tracked-set binding comes from the host context` | done | `ERP:wildcard_any_tracked_same_timestamp_dedup`, `RTP:wildcard_tracks_string_and_event_changes`, `wildcard_tracked_set_binding_comes_from_host_context` | Code-only test reuses one bound `*` against multiple tracked sets because shared JSON cases bind per case. |
| 8 | `[1.2] Simple signal name resolution` | done | `PP:named_any_change`, `ERP:named_change_requires_previous_sample`, `RTP:named_string_event_matches_signal_change` | - |
| 9 | `[1.2] Hierarchical name resolution` | done | `PP:hierarchical_named_any_change`, `ERP:hierarchical_named_change_resolution` | - |
| 10 | `[1.2] Canonical dump-token resolution` | done | `PP:canonical_token_named_any_change`, `ERP:canonical_token_named_change_resolution` | Uses a bracketed canonical dump token outside the simple/hierarchical-name cases. |
| 11 | `[1.2] Unresolved or non-signal names are errors` | done | `ERN:unknown_signal_named_event`, `ERN:unknown_signal_inside_iff`, `IBN:unresolved_signal_in_event_iff` | Direct unknown whole-event operands are now covered. Shared JSON host fixtures only expose signals, so there is no separate non-signal target fixture to add today. |
| 12 | `[1.3] Named event means any value change` | done | `ERP:named_change_requires_previous_sample`, `RTP:named_string_event_matches_signal_change`, `RTP:named_event_operand_matches_raw_event_timestamp` | - |
| 13 | `[1.3] Wildcard event means any tracked-set change` | done | `ERP:wildcard_any_tracked_same_timestamp_dedup`, `RTP:wildcard_tracks_string_and_event_changes` | - |
| 14 | `[1.3] Edge sampling uses the previous sample strictly before the candidate timestamp` | done | `ERP:edge_uses_previous_sample_strictly_before_candidate`, `ERP:edge_terms_use_lsb_and_xz_normalization` | - |
| 15 | `[1.3] Only the least-significant bit participates in edge classification` | done | `ERP:edge_terms_use_lsb_and_xz_normalization`, `ERP:named_event_matches_non_lsb_bit_changes`, `ERP:posedge_ignores_non_lsb_bit_changes`, `ERP:negedge_ignores_non_lsb_bit_changes`, `ERP:edge_ignores_non_lsb_bit_changes` | - |
| 16 | `[1.3] Nine-state h/u/w/l/- normalization before edge classification` | done | `ERP:edge_terms_use_lsb_and_xz_normalization`, `ERP:edge_terms_normalize_u_w_l_and_dash_aliases` | Legacy case covers `h`; direct fixture coverage now locks `u`, `w`, `l`, and `-`. |
| 17 | `[1.3] posedge classification matrix` | done | `ERP:edge_terms_use_lsb_and_xz_normalization`, `ERP:posedge_classification_matrix`, `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface` | Direct fixture now enumerates the full 4-state transition matrix, including non-matching controls. |
| 18 | `[1.3] negedge classification matrix` | done | `ERP:edge_terms_use_lsb_and_xz_normalization`, `ERP:negedge_classification_matrix`, `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface` | Direct fixture now enumerates the full 4-state transition matrix, including non-matching controls. |
| 19 | `[1.3] edge is the union of posedge and negedge` | done | `PP:edge_keywords`, `ERP:edge_is_union_of_posedge_and_negedge`, `ERP:edge_terms_use_lsb_and_xz_normalization`, `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface` | Direct fixture now enumerates both edge matrices plus non-edge controls in one exact-match case. |
| 20 | `[1.3] No previous sample means no edge` | done | `ERP:posedge_requires_previous_sample`, `ERP:negedge_requires_previous_sample`, `ERP:edge_requires_previous_sample`, `ERP:edge_terms_use_lsb_and_xz_normalization` | Direct fixtures now isolate first-sample no-edge behavior for each edge operator. |
| 21 | `[1.4] Union is logical OR over event terms` | done | `ERP:wildcard_any_tracked_same_timestamp_dedup`, `ERP:iff_binds_only_to_preceding_term`, `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface` | Verified across rhs-only, lhs-only, and same-timestamp-overlap union matches. |
| 22 | `[1.4] or and , are exact semantic synonyms` | done | `PP:or_union`, `PP:comma_union`, `ERP:or_union_semantic_synonym_of_comma`, `ERP:comma_union_semantic_synonym_of_or`, `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface` | Dedicated parse and runtime fixture pairs now lock identical term structure and match sets for both union spellings. |
| 23 | `[1.4] Same-timestamp union matches are deduplicated` | done | `ERP:wildcard_any_tracked_same_timestamp_dedup` | Verified by the existing fixture: `*` and `posedge clk` both match at timestamp 5, but the runtime result keeps that timestamp only once. |
| 24 | `[1.4] iff gates only the immediately preceding event term` | done | `PP:iff_binds_to_preceding_term_only`, `ERP:iff_binds_only_to_preceding_term`, `typed_parser_preserves_iff_binding_to_single_term`, `event_expr_iff_binding_with_union` | Verified across fixture-backed parse shape and runtime match behavior, with parser and legacy regression tests locking the same binding rule. |
| 25 | `[1.4] Example grouping for negedge clk iff rstn or ready` | done | `PP:iff_binds_to_preceding_term_only`, `ERP:iff_binds_only_to_preceding_term`, `typed_parser_preserves_iff_binding_to_single_term`, `event_expr_iff_binding_with_union` | Verified across the exact parse fixture spelling plus runtime, typed-parser, and legacy checks for the same binding rule. |
| 26 | `[1.4] iff uses the boolean expression language from section 2` | done | `ERP:iff_bounded_ops_and_literals`, `IBP:iff_gate_arithmetic_and_cast_standalone_false`, `IBP:iff_gate_arithmetic_and_cast_standalone_true`, `IBP:iff_arithmetic_and_cast_in_event_engine`, `IBP:iff_inside_and_concatenation`, `IBP:iff_dynamic_selection_and_conditional`, `RTP:rich_iff_with_triggered_real_and_string` | Direct standalone logical fixtures now mirror the same arithmetic-and-cast gate that the event fixture uses, locking identical false/true outcomes before and during the gated posedges. |
| 27 | `[1.4] Event expressions have no independent parenthesized grouping form` | done | `PP:iff_parenthesized_payload`, `PN:parenthesized_event_union_remains_invalid`, `ERN:event_grouping_remains_invalid`, `IBN:event_level_grouping_remains_invalid`, `event_expr_iff_capture_parenthesized_logical_payload` | Direct parse coverage now locks both the accepted `iff (...)` payload form and the exact rejected `(posedge clk or ready)` event-grouping spelling from the checklist. |
| 28 | `[1.5] Union is the only event-composition operator` | done | `PN:event_level_word_operator_remains_invalid`, `PN:event_level_symbolic_operator_remains_invalid`, `ERN:event_grouping_remains_invalid`, `IBN:event_level_grouping_remains_invalid` | Parse fixtures now reject both word and symbolic top-level non-union operators in addition to grouped event syntax. |
| 29 | `[1.5] iff binds tighter than union` | done | `PP:iff_binds_to_preceding_term_only`, `PP:rhs_iff_binds_tighter_than_union`, `ERP:iff_binds_only_to_preceding_term`, `ERP:rhs_iff_binds_tighter_than_union`, `typed_parser_preserves_iff_binding_to_single_term` | Direct parse and runtime fixtures now lock both `a iff cond or b` and `a or b iff cond`, so the precedence check is no longer inferred from one-sided coverage. |
| 30 | `[1.5] or and , have equal precedence and left-to-right association` | done | `PP:comma_union`, `PP:mixed_union_left_to_right_chain`, `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface`, `event_expr_accepts_comma_union` | Direct mixed-spelling parse coverage now locks the flat three-term left-to-right AST shape. |
| 31 | `[1.5] Event precedence order is basic event, then gated term, then union` | done | `PP:iff_binds_to_preceding_term_only`, `ERP:iff_binds_only_to_preceding_term`, `typed_parser_preserves_iff_binding_to_single_term` | - |
| 32 | `[1.6] Event grammar accepts repeated unions of event terms` | done | `PP:edge_keywords`, `PP:comma_union`, `PP:mixed_union_four_term_chain`, `PP:iff_binds_to_preceding_term_only` | - |
| 33 | `[1.6] event_term is limited to basic_event and basic_event iff logical_expr` | done | `PP:named_event_iff_gate`, `PP:iff_binds_to_preceding_term_only`, `ERN:event_grouping_remains_invalid` | Direct parse fixtures now cover both accepted gated-term entry points and the rejected grouped-union form. |
| 34 | `[1.6] basic_event grammar matrix` | done | `PP:wildcard_any_tracked`, `PP:named_any_change`, `PP:edge_keywords` | Verified: the existing parse fixtures cover `*`, a plain operand reference, and each edge-keyword form with an operand reference. |
| 35 | `[1.6] Edge keywords require operand references, not arbitrary expressions` | done | `PN:posedge_literal_operand_remains_invalid`, `PN:posedge_parenthesized_operand_remains_invalid`, `PN:negedge_literal_operand_remains_invalid`, `PN:negedge_parenthesized_operand_remains_invalid`, `PN:edge_literal_operand_remains_invalid`, `PN:edge_parenthesized_operand_remains_invalid` | Direct parse fixtures now reject both literal and parenthesized non-reference operands for all three edge keywords. |

### Types, Primaries, and Casts (`36`-`92`)

| ID | Checklist point | Status | Current tests | Notes |
| --- | --- | --- | --- | --- |
| 36 | `[2.0] Inline vector cast targets are supported` | done | `IBP:integral_cast_bit_logic_variants`, `IBP:integral_cast_scalar_bit_target`, `IBP:integral_cast_scalar_logic_target`, `IBP:integral_cast_signed_bit_unsigned_logic_targets`, `IBP:integral_cast_unsigned_bit_signed_logic_targets`, `IBP:integral_cast_signed_logic_unsigned_bit_targets`, `IBP:integral_cast_unsigned_logic_signed_bit_targets` | Verified: the integral/boolean positive manifest now exercises each bare, signed, and unsigned inline bit/logic cast-target spelling directly. |
| 37 | `[2.0] Dump-derived operand-type casts are supported` | done | `RTP:operand_type_cast_bit_vector`, `RTP:operand_type_cast_integer_like`, `RTP:operand_type_cast_real`, `RTP:operand_type_cast_string`, `RTP:enum_cast_reacquires_label` | Verified: rich-type fixtures cover `type(ref)'(...)` across non-enum recovered targets plus an enum-typed operand reference, so both documented operand-reference classes use the referenced metadata as the cast target. |
| 38 | `[2.0] Dump-derived enum label references are supported` | done | `RTP:enum_label_reference`, `RTN:invalid_enum_label_on_non_enum`, `RTN:invalid_missing_enum_label` | Verified: the rich-type fixtures already cover successful `type(enum_operand_reference)::LABEL` primary use plus both documented semantic failures for non-enum operands and missing labels. |
| 39 | `[2.1] Scalar bit and logic metadata` | done | `RTP:scalar_bit_primary_preserves_metadata`, `RTP:scalar_logic_primary_preserves_metadata`, `RTP:triggered_logical_primary` | Verified: direct rich-type primaries now isolate scalar 2-state vs 4-state metadata, and `.triggered` still covers the documented scalar `bit` result. |
| 40 | `[2.1] Packed bit-vector family metadata matrix` | done | `IBP:integral_cast_bit_logic_variants`, `IBP:integral_cast_bit_vector_target_width3`, `IBP:integral_cast_logic_vector_target_width5`, `IBP:integral_cast_signed_bit_unsigned_logic_targets`, `IBP:integral_cast_unsigned_bit_signed_logic_targets`, `IBP:integral_cast_signed_logic_unsigned_bit_targets`, `IBP:integral_cast_unsigned_logic_signed_bit_targets` | Verified: packed-vector cast fixtures now directly cover plain, signed, and unsigned `bit`/`logic` families, with width checks beyond the original width-4 samples. |
| 41 | `[2.1] Integer-like builtin metadata matrix` | done | `IBP:integral_cast_integer_like_target`, `IBP:operand_type_cast_byte_metadata`, `IBP:operand_type_cast_shortint_metadata`, `IBP:operand_type_cast_longint_metadata`, `IBP:operand_type_cast_integer_metadata`, `IBP:operand_type_cast_time_metadata`, `IBP:integer_like_add_byte_preserves_metadata`, `IBP:integer_like_add_shortint_preserves_metadata`, `IBP:integer_like_add_int_preserves_metadata`, `IBP:integer_like_add_longint_preserves_metadata`, `IBP:integer_like_add_integer_preserves_metadata`, `IBP:integer_like_add_time_preserves_metadata`, `RTP:operand_type_cast_integer_like` | Operand-typed casts plus same-kind arithmetic now cover the full integer-like builtin family, including the documented width, signedness, and 2-state/4-state differences. |
| 42 | `[2.1] Enum type metadata and label carriage` | done | `RTP:enum_label_reference`, `RTP:enum_cast_reacquires_label`, `RTP:enum_primary_preserves_unlabeled_identity`, `RTP:two_state_enum_primary_preserves_label`, `IBP:enum_conditional_preserves_identity` | Verified: rich-type fixtures now cover labeled and unlabeled enum values plus direct 2-state enum label carriage, so enum metadata survives with the expected carried label state across both enum domains. |
| 43 | `[2.1] Real type metadata` | done | `RTP:real_mixed_numeric_result`, `RTP:real_power_result`, `RTP:operand_type_cast_real`, `RTP:real_cast_truncates_to_int` | Verified: existing rich-type fixtures assert `real` kind plus width-64 metadata for real literals, mixed numeric operators, and operand-type casts, and separately lock real-to-integral conversion behavior. |
| 44 | `[2.1] String type metadata` | done | `RTP:string_conditional_result`, `RTP:string_equality_exact`, `RTP:string_equality_different_contents`, `RTP:string_equality_different_lengths`, `RTN:invalid_string_numeric_add`, `RTN:invalid_string_boolean_context` | Verified: string fixtures now lock direct string result metadata plus equality over exact-match, different-content, and different-length byte sequences, while negative cases keep string values out of numeric and boolean contexts. |
| 45 | `[2.1] Event operand and .triggered` | done | `RTP:triggered_logical_primary`, `RTN:invalid_raw_event_value_use`, `RTN:invalid_event_cast` | Verified: `.triggered` is covered as a logical primary with direct 2-state `bit` metadata, while raw `event` value use and raw-event casts are both rejected. |
| 46 | `[2.1] Raw event is excluded from ordinary value operations` | done | `RTN:invalid_raw_event_value_use`, `RTN:invalid_event_cast`, `RTN:invalid_raw_event_implicit_conversion`, `RTN:invalid_raw_event_selection`, `RTN:invalid_raw_event_concatenation`, `RTN:invalid_raw_event_replication` | Direct rich-type negatives now sample each documented forbidden category until `.triggered` is applied. |
| 47 | `[2.2.1] Integral literal surface-form matrix` | done | `IBP:integral_literal_surface_form_matrix`, `ERP:iff_bounded_ops_and_literals`, `IBP:precedence_and_associativity_core` | Verified: the integral/boolean positive manifest now parses and evaluates representative unsized decimal, unsized based, and sized based literals from the checklist in one direct fixture. |
| 48 | `[2.2.1] Integral literal sign and width defaults` | done | `IBP:integral_literal_decimal_signed_default`, `IBP:integral_literal_based_decimal_unsigned_default`, `IBP:integral_literal_based_decimal_signed_default`, `IBP:integral_literal_based_hex_unsigned_default`, `IBP:integral_literal_sized_hex_unsigned_default`, `IBP:integral_literal_sized_hex_width_and_sign_default` | Verified: direct logical-eval literal fixtures now lock signed decimal, unsigned vs signed unsized based decimal, unsigned unsized hex, and both unsigned and signed size-prefixed hex defaults via explicit type-width/sign assertions. |
| 49 | `[2.2.1] Based integral literals may contain x and z` | done | `IBP:integral_cast_logic_vector_target_width5`, `IBP:integral_cast_scalar_logic_target`, `IBP:operand_type_cast_integer_metadata` | Verified: existing logical-eval fixtures directly accept based literals containing both `x` and `z` digits, with packed-vector and integer-like 4-state results preserving both bits across casts and a scalar `logic` cast separately covering `z`. |
| 50 | `[2.2.1] Integral literals are self-determined` | done | `IBP:integral_literal_self_determined_width_before_common_type`, `IBP:integral_literal_self_determined_signed_extension_before_common_type`, `IBP:integral_literal_self_determined_unsigned_zero_extension_before_common_type` | Verified: direct width and signed-vs-unsigned extension cases now isolate literal self-determination before common-type conversion. |
| 51 | `[2.2.1] Unary minus is an operator, not part of the literal token` | done | `UT:logical_parser_keeps_unary_minus_separate_from_integral_literals` | Verified: parser unit coverage now asserts both `-12` and `-(12)` produce unary-minus AST nodes over positive decimal literal tokens, with literal spans excluding the leading `-`. |
| 52 | `[2.2.1] Real literals produce self-determined real` | done | `RTP:real_literal_self_determined`, `RTP:real_mixed_numeric_result`, `RTP:real_power_result` | Verified: a direct bare-literal fixture now locks standalone `real` type/result metadata, while the existing mixed-numeric and exponentiation cases keep the literal self-determined in composed expressions. |
| 53 | `[2.2.1] String literals produce self-determined string` | done | `RTP:string_literal_self_determined`, `RTP:string_conditional_result`, `RTP:string_equality_exact` | Direct standalone rich-type coverage now locks the bare literal's `string` type and payload instead of only observing literals through conditional/equality contexts. |
| 54 | `[2.2.2] Operand-reference primaries use dump-recovered operand types` | done | `RTP:operand_reference_primary_bit_vector`, `RTP:operand_reference_primary_integer_like`, `RTP:operand_reference_primary_real`, `RTP:operand_reference_primary_string`, `RTP:operand_reference_primary_enum` | Direct standalone operand-reference fixtures now lock recovered bit-vector, integer-like, real, string, and enum metadata without relying on cast or enum-label indirection. |
| 55 | `[2.2.2] Literals are valid primary expressions` | done | `IBP:integral_literal_decimal_signed_default`, `IBP:integral_literal_based_decimal_unsigned_default`, `IBP:integral_literal_sized_hex_width_and_sign_default`, `IBP:integral_literal_surface_form_matrix`, `IBP:precedence_and_associativity_core`, `RTP:real_literal_self_determined`, `RTP:real_power_result`, `RTP:string_literal_self_determined`, `RTP:string_equality_exact` | Verified: standalone integral decimal, unsized based, sized based, real, and string literal fixtures cover primary acceptance directly, and existing composed-expression cases keep each literal kind valid as an operator operand. |
| 56 | `[2.2.2] Parenthesized expressions preserve inner value and type` | done | `RTP:parenthesized_string_literal_preserves_value_and_type`, `RTP:parenthesized_enum_primary_preserves_label_and_type` | Direct rich-type fixtures now lock both standalone and label-carrying parenthesized values without changing the inner type or payload. |
| 57 | `[2.2.2] Static casts are valid primaries` | done | `IBP:integral_cast_primary_binds_before_addition`, `RTP:operand_type_cast_real_primary_binds_before_addition`, `IBP:integral_cast_integer_like_target`, `RTP:real_cast_truncates_to_int`, `RTP:operand_type_cast_real` | Direct arithmetic fixtures now place both ordinary and operand-type casts on the left side of infix addition, locking that each cast form is accepted as a primary operand while existing standalone cases keep target metadata and conversion behavior pinned. |
| 58 | `[2.2.2] Enum label reference primary semantics` | done | `RTP:enum_label_reference` | Verified: the direct rich-type fixture evaluates `type(state)::BUSY` through dump-recovered enum metadata and asserts the result stays enum-typed with the expected carried label. |
| 59 | `[2.2.2] Enum label reference invalid cases` | done | `RTN:invalid_enum_label_on_non_enum`, `RTN:invalid_missing_enum_label`, `RTN:invalid_missing_enum_metadata` | Verified: rich-type negative fixtures now directly reject enum-label primaries on non-enum operands, missing enum labels, and dump-backed enum references whose recovered metadata lacks a label table. |
| 60 | `[2.2.2] Selection primary syntax matrix` | done | `IBP:dynamic_bit_select_valid`, `IBP:part_select_and_indexed_part_select`, `IBP:indexed_down_part_select_valid` | Direct logical-eval fixtures now cover bit select, static part select, indexed up-select, and indexed down-select so all four postfix selection forms are accepted. |
| 61 | `[2.2.2] Concatenation primary syntax` | done | `IBP:concat_replication_inside_conditional`, `IBP:concatenation_select_uses_derived_width`, `IBP:iff_inside_and_concatenation` | Verified: existing logical and event fixtures accept concatenations both as standalone selected primaries and as nested operands inside conditional/`iff` contexts. |
| 62 | `[2.2.2] Replication primary syntax` | done | `IBP:concat_replication_inside_conditional`, `IBP:replication_select_indexes_replicated_bits`, `IBN:invalid_const_replication_multiplier` | Verified: the current fixture set accepts `{N{expr}}` as a primary with constant multiplier `2` in both nested and standalone contexts, while `invalid_const_replication_multiplier` pins the constant-count guardrail; zero and broader multiplier matrixing remain intentionally unexpanded here. |
| 63 | `[2.2.2] .triggered is the only supported member-like primary` | done | `RTP:triggered_logical_primary`, `RTN:invalid_non_triggered_member_suffix`, `RTN:invalid_triggered_on_non_event` | Verified: the rich-type fixtures now accept `.triggered` and directly reject another member-like suffix via `(ev).foo`. |
| 64 | `[2.2.2] .triggered is restricted to raw event references and may not chain` | done | `RTP:triggered_logical_primary`, `RTN:invalid_triggered_on_non_event`, `RTN:invalid_chained_triggered` | Verified: `triggered_logical_primary` accepts `.triggered` only on the raw `ev` event reference, `invalid_triggered_on_non_event` rejects the same suffix on non-event `data`, and `invalid_chained_triggered` locks the chained-form rejection. |
| 65 | `[2.3.1] Cast syntax` | done | `IBP:integral_cast_integer_like_target`, `IBP:integral_cast_bit_logic_variants`, `IBP:signed_division_preserves_twos_complement_result`, `RTP:string_conditional_result`, `RTN:invalid_real_cast_from_unknown_bits`, `RTP:operand_type_cast_bit_vector`, `IBN:invalid_cast_target_width` | Verified: existing fixtures accept the documented `type'(expr)` shape across representative integer-like, inline-vector, signedness-only, string, real, and recovered-type targets, and reject the malformed `bit[0]'(...)` target form. |
| 66 | `[2.3.2] Supported explicit cast-target families` | done | `IBP:integral_cast_bit_logic_variants`, `IBP:integral_cast_integer_like_target`, `IBP:integral_cast_unsigned_target_family`, `RTP:real_cast_target_family`, `RTP:string_conditional_result`, `RTP:operand_type_cast_real` | Direct fixtures now cover inline `bit/logic[...]`, integer-like, bare `unsigned`, explicit `real`/`string`, and recovered `type(operand)` cast-target families. |
| 67 | `[2.3.3] Casts may change width, signedness, state domain, and operand kind` | done | `IBP:integral_cast_changes_width_signedness_and_state_domain`, `IBP:integral_cast_integer_like_target`, `IBP:integral_cast_bit_logic_variants`, `RTP:operand_type_cast_real` | Direct integral coverage now locks a single cast that simultaneously changes width, signedness, and state domain, while the existing real cast keeps operand-kind conversion explicit. |
| 68 | `[2.3.3] Integral casts use deterministic bit-level conversion` | done | `IBP:integral_cast_integer_like_target`, `IBP:integral_cast_bit_logic_variants`, `IBP:integral_cast_deterministic_signed_source_bits`, `IBP:integral_cast_deterministic_unsigned_source_bits` | Direct widening casts now isolate that the source bit pattern plus source signedness deterministically drive the widened result rather than the target signedness. |
| 69 | `[2.3.4] Signedness-only casts are width-preserving reinterpretations` | done | `IBP:signed_cast_reinterprets_unsigned_bits_without_resize`, `IBP:unsigned_cast_reinterprets_signed_bits_without_resize` | Direct 2-state fixtures now isolate both `signed'` and `unsigned'` as pure reinterpret casts that flip signedness metadata while preserving operand width and raw bits. |
| 70 | `[2.3.4] Signedness-only casts do not change the state domain` | done | `IBP:signed_cast_preserves_four_state_domain`, `IBP:unsigned_cast_preserves_four_state_domain` | Direct 4-state fixtures now isolate both `signed'` and `unsigned'` as signedness-only casts that preserve the source state domain and raw bits. |
| 71 | `[2.3.4] Signedness-only casts are valid only for integral operands` | done | `IBP:signed_cast_reinterprets_unsigned_bits_without_resize`, `IBP:unsigned_cast_reinterprets_signed_bits_without_resize`, `RTP:unsigned_cast_accepts_integer_like_operand`, `RTP:signed_cast_accepts_enum_operand` | Verified: direct signedness-only cast fixtures now cover each accepted integral operand family: bit-vector, integer-like, and enum. |
| 72 | `[2.3.4] Signedness-only casts reject real, string, and event` | done | `RTN:invalid_signed_cast_from_real`, `RTN:invalid_unsigned_cast_from_real`, `RTN:invalid_signed_cast_from_string`, `RTN:invalid_unsigned_cast_from_string`, `RTN:invalid_signed_cast_from_event`, `RTN:invalid_unsigned_cast_from_event` | Direct rich-type negatives now lock both signedness-only cast keywords against real, string, and raw event operands. |
| 73 | `[2.3.5] Widening resize extension rules` | done | `IBP:integral_cast_integer_like_target`, `IBP:integral_cast_integer_like_target_zero_extends_unsigned_source` | Direct paired fixtures now lock both widening branches against the same `int` target: signed sources sign-extend, and unsigned sources zero-extend. |
| 74 | `[2.3.5] Narrowing integral casts keep least-significant bits` | done | `IBP:integral_cast_narrowing_keeps_least_significant_bits` | Verified: a direct same-family `logic[8] -> logic[4]` cast fixture now isolates narrowing and locks retention of the trailing four source bits. |
| 75 | `[2.3.5] Post-resize signedness comes from the target type` | done | `IBP:integral_cast_post_resize_signedness_comes_from_signed_target`, `IBP:integral_cast_post_resize_signedness_comes_from_unsigned_target` | Direct paired casts narrow `8'hff` to the same 4-bit pattern under signed vs unsigned 4-bit targets, then widen to `int` so the signed resized value sign-extends while the unsigned resized value zero-extends. |
| 76 | `[2.3.6] Integral state-domain conversion rules` | done | `IBP:integral_cast_two_state_target_zeroes_unknowns`, `IBP:integral_cast_four_state_target_preserves_unknowns`, `IBP:integral_cast_bit_logic_variants` | Direct paired fixtures now isolate both branches of state-domain conversion from the same 4-state source: `bit[...]` zeroes `x/z`, while `logic[...]` preserves them. |
| 77 | `[2.3.7] Inline vector cast-target semantics matrix` | done | `IBP:integral_cast_inline_vector_width3_bit_target`, `IBP:integral_cast_inline_vector_width3_logic_target`, `IBP:integral_cast_inline_vector_width3_signed_bit_target`, `IBP:integral_cast_inline_vector_width3_signed_logic_target`, `IBP:integral_cast_inline_vector_width3_unsigned_bit_target`, `IBP:integral_cast_inline_vector_width3_unsigned_logic_target`, `IBP:integral_cast_inline_vector_width5_bit_target`, `IBP:integral_cast_inline_vector_width5_logic_target`, `IBP:integral_cast_inline_vector_width5_signed_bit_target`, `IBP:integral_cast_inline_vector_width5_signed_logic_target`, `IBP:integral_cast_inline_vector_width5_unsigned_bit_target`, `IBP:integral_cast_inline_vector_width5_unsigned_logic_target` | Direct width-3 and width-5 fixture matrices now cast the same `4'b10xz` source into all six inline vector target families and lock the documented width, signedness, and 2-state/4-state semantics. |
| 78 | `[2.3.7] bit and logic are width-1 aliases` | done | `IBP:integral_cast_scalar_bit_target`, `IBP:integral_cast_scalar_bit_width1_target`, `IBP:integral_cast_scalar_logic_target`, `IBP:integral_cast_scalar_logic_width1_target`, `IBP:integral_cast_scalar_bit_alias_concat_bare`, `IBP:integral_cast_scalar_bit_alias_concat_width1`, `IBP:integral_cast_scalar_logic_alias_concat_bare`, `IBP:integral_cast_scalar_logic_alias_concat_width1` | Direct scalar cast pairs now lock `bit`/`bit[1]` and `logic`/`logic[1]` to identical width-1 result types, and paired concatenation fixtures confirm the aliases behave identically as operator operands. |
| 79 | `[2.3.8] Operand-type casts use recovered operand metadata` | done | `RTP:operand_type_cast_bit_vector`, `RTP:operand_type_cast_integer_like`, `RTP:operand_type_cast_real`, `RTP:operand_type_cast_string` | Verified: the rich-type fixtures assert both the cast result type and value against the referenced operand's recovered bit-vector, integer-like, real, and string metadata. |
| 80 | `[2.3.8] Operand-type casts support non-enum recovered types` | done | `RTP:operand_type_cast_bit_vector`, `RTP:operand_type_cast_integer_like`, `RTP:operand_type_cast_real`, `RTP:operand_type_cast_string` | Verified: the existing rich-type fixtures already cover operand-type casts that recover non-enum bit-vector, integer-like, real, and string target types. |
| 81 | `[2.3.8] Operand-type casts are the primary route into dump-derived enum types` | done | `RTP:enum_cast_reacquires_label` | Verified: `type(state)'(2'b01)` casts through the dump-recovered enum metadata for `state` and the fixture asserts both the enum-typed result and the reacquired `BUSY` label. |
| 82 | `[2.3.9] Enum label references are primaries, not cast targets` | done | `RTP:enum_label_reference`, `RTN:invalid_enum_label_on_non_enum`, `RTN:invalid_enum_label_cast_target` | Verified: the direct primary fixture still resolves `type(state)::BUSY`, and a dedicated negative now rejects `type(state)::BUSY'(2'b00)` as an invalid cast target. |
| 83 | `[2.3.10] Enum casts use underlying bit-vector values` | done | `RTP:enum_cast_reacquires_label`, `RTP:signed_cast_accepts_enum_operand` | Verified: the rich-type fixtures cover both casting an integral bit pattern into an enum and casting an enum operand back out through an integral signedness-only cast, so enum conversion is exercised on the underlying bits in both directions. |
| 84 | `[2.3.10] Enum casts apply target enum width and label set` | done | `RTP:enum_cast_applies_target_width`, `RTP:enum_cast_applies_target_label_set` | Direct paired casts now send the same `3'b101` source through a 2-bit `state` enum and a 3-bit `phase` enum, locking both target-driven resize and target-specific label resolution. |
| 85 | `[2.3.10] Enum casts keep labels on exact bit-pattern matches` | done | `RTP:enum_cast_reacquires_label` | Verified: `type(state)'(2'b01)` returns the enum-typed `fsm_state` result with the matching `BUSY` label preserved on the exact target bit pattern. |
| 86 | `[2.3.10] Enum casts yield unlabeled enum values on non-matching patterns` | done | `RTP:enum_cast_yields_unlabeled_value_on_non_matching_pattern` | Direct rich-type fixture now casts `2'b11` into the 2-bit `state` enum and asserts that the result keeps the enum type while carrying no label because `11` is not declared. |
| 87 | `[2.3.11] real'(integral) is numeric conversion from the source's self-determined value` | done | `RTP:operand_type_cast_real`, `RTP:real_cast_uses_self_determined_unsigned_source_value`, `RTP:real_cast_uses_self_determined_signed_source_value`, `RTP:real_mixed_numeric_result` | Direct paired casts now narrow the same `8'hff` source to unsigned vs signed 3-bit values before converting to `real`, locking that the cast uses the source's self-determined width and signedness. |
| 88 | `[2.3.11] real'(integral) rejects x and z` | done | `RTN:invalid_real_cast_from_unknown_bits`, `RTN:invalid_real_cast_from_z_bits` | Direct paired runtime negatives now cover both signal-backed `x` input and explicit `1'bz` input, locking rejection of unknown and high-impedance integral sources. |
| 89 | `[2.3.11] Real-to-integral casts truncate toward zero before target rules` | done | `RTP:real_cast_truncates_to_int`, `RTP:real_cast_truncates_negative_to_int`, `RTP:real_cast_truncates_negative_to_unsigned_bit3`, `RTP:real_cast_truncates_negative_to_signed_bit3`, `RTP:real_cast_truncates_negative_to_logic3` | Direct fixtures now pair positive/negative `int` casts with width-3 unsigned, signed, and four-state vector casts, locking truncation toward zero before resize, signedness, and target-family rules apply. |
| 90 | `[2.3.12] string'(string) identity cast` | done | `RTP:string_identity_cast_literal`, `RTP:string_identity_cast_operand` | Direct paired rich-type fixtures now cast both a string literal and a string operand through `string'(...)`, locking acceptance as an identity cast that preserves the original string value. |
| 91 | `[2.3.12] Other explicit string casts are invalid` | done | `RTN:invalid_string_cast_from_integral`, `RTN:invalid_string_cast_from_real`, `RTN:invalid_real_cast_from_string`, `RTN:invalid_int_cast_from_string` | Direct rich-type negatives now lock both documented non-identity directions: integral/real to `string` and `string` to integral/real. |
| 92 | `[2.3.13] Raw event is not castable, but .triggered follows ordinary integral cast rules` | done | `RTN:invalid_event_cast`, `RTP:triggered_logical_primary`, `RTP:triggered_int_cast_primary` | Direct paired coverage now rejects `bit'(ev)` while accepting `int'(ev.triggered)` as a normal integral cast. |

### Conversion and Typing Rules (`93`-`108`)

| ID | Checklist point | Status | Current tests | Notes |
| --- | --- | --- | --- | --- |
| 93 | `[2.4] Common-type conversion is the default outside self-determined operands` | done | `IBP:integral_common_type_default_addition_uses_max_width_sign_and_state`, `IBP:integral_common_type_default_comparison_uses_converted_operands`, `IBP:integral_common_type_default_equality_preserves_four_state_operands`, `RTP:real_mixed_numeric_result`, `IBP:integral_literal_self_determined_width_before_common_type`, `IBP:integral_literal_self_determined_signed_extension_before_common_type`, `IBP:integral_literal_self_determined_unsigned_zero_extension_before_common_type`, `RTP:real_cast_uses_self_determined_unsigned_source_value`, `RTP:real_cast_uses_self_determined_signed_source_value` | Direct arithmetic, comparison, and equality fixtures now isolate default common-type coercion across width/sign/state mismatches, while existing literal and real-cast fixtures keep the self-determined exceptions pinned. |
| 94 | `[2.4.1] Boolean-context consumers` | done | `IBP:boolean_context_not_reuses_integral_truth_value`, `IBP:boolean_context_and_reuses_integral_truth_value`, `IBP:boolean_context_or_reuses_integral_truth_value`, `IBP:boolean_context_ternary_condition_reuses_integral_truth_value` | Direct integral fixtures now reuse the same 4-bit operand under `!`, `&&`, `||`, and `?:`, and each context collapses it to the same temporary 1-bit truth result. |
| 95 | `[2.4.1] Integral boolean-context truth table` | done | `IBP:boolean_context_integral_zero_is_false`, `IBP:boolean_context_integral_nonzero_is_true`, `IBP:boolean_context_integral_x_is_unknown`, `IBP:boolean_context_integral_z_is_unknown`, `IBP:boolean_context_integral_mixed_unknown_vector_is_unknown` | Direct integral fixtures now parameterize `0`, non-zero, `x`, `z`, and mixed unknown-containing vectors with exact `0`/`1`/`x` truth results. |
| 96 | `[2.4.1] Real boolean-context truth table` | done | `RTP:real_boolean_context_zero_is_false`, `RTP:real_logical_or_value`, `RTP:real_boolean_context_negative_nonzero_is_true` | Existing `0.0 || temp` coverage plus new `temp || 0.0` fixtures now cover `0.0`, positive non-zero, and negative non-zero real operands, locking the documented false/true split for real boolean conversion. |
| 97 | `[2.4.1] Strings do not implicitly convert in boolean context` | done | `RTN:invalid_string_boolean_context` | Verified: the rich-type negative fixture directly uses the string operand `msg` in `msg && 1` and expects semantic rejection via `EXPR-SEMANTIC-BOOLEAN-CONTEXT`. |
| 98 | `[2.4.1] Boolean conversion is temporary 1-bit truth only` | done | `IBP:boolean_context_truth_is_temporary_not_width_erasing` | Direct integral coverage now reuses the same 8-bit operand as both the ternary condition and the selected arm, proving boolean-context conversion stays temporary while the operand keeps its original width/type in neighboring non-boolean evaluation. |
| 99 | `[2.4.2] Integral common-type family and default width rule` | done | `IBP:integral_common_type_default_addition_uses_max_width_sign_and_state`, `IBP:integral_common_type_default_comparison_uses_converted_operands`, `IBP:integral_common_type_default_equality_preserves_four_state_operands`, `IBP:integral_common_type_default_integer_like_addition_uses_max_width` | Direct arithmetic, comparison, and equality fixtures now isolate the default max-width rule across mixed-width bit-vector operands and a mixed integer-like/bit-vector arithmetic pair. |
| 100 | `[2.4.2] Integral common sign and state rules` | done | `IBP:integral_common_type_sign_state_signed_two_state_addition`, `IBP:integral_common_type_sign_state_signed_four_state_addition`, `IBP:integral_common_type_sign_state_unsigned_two_state_addition`, `IBP:integral_common_type_sign_state_unsigned_four_state_addition` | Direct same-width addition fixtures now cover the full 2x2 sign/state matrix, locking signed-only vs mixed-unsigned and two-state vs mixed-four-state common-type outcomes. |
| 101 | `[2.4.2] Integral extension and domain-conversion rules` | done | `IBP:integral_common_type_signed_extension_before_case_equality`, `IBP:integral_common_type_unsigned_extension_before_case_equality`, `IBP:integral_common_type_four_state_conversion_preserves_xz_before_case_equality`, `IBP:integral_cast_two_state_target_zeroes_unknowns` | Direct case-equality fixtures now isolate signed sign-extension, unsigned zero-extension, and 4-state `x/z` preservation during implicit common-type coercion, while the existing cast fixture keeps the 2-state `x/z -> 0` rule pinned. |
| 102 | `[2.4.3] Enum participation and label-loss rules` | done | `IBP:enum_common_type_ignores_labels_for_bitwise_or`, `IBP:enum_conditional_preserves_identity`, `binder_preserves_enum_identity_in_conditional_arms` | Direct bitwise-OR coverage now mixes a labeled 2-state enum with a wider signed 4-state vector, proving common-type coercion uses the enum's underlying bits and drops labels while selecting width/sign/state. |
| 103 | `[2.4.3] ?: preserves enum type only for matching enum arms` | done | `IBP:enum_conditional_preserves_identity`, `IBP:enum_conditional_mismatched_arms_drop_identity`, `IBP:conditional_unknown_condition_merges_bits`, `RTP:enum_conditional_merges_unknown_identity`, `binder_preserves_enum_identity_in_conditional_arms` | Direct ternary fixtures now cover both sides of the rule: matching enum arms preserve the enum type, while mismatched enum arms drop identity and fall back to the integral common type. |
| 104 | `[2.4.4] Real-family common-type rules` | done | `RTP:real_mixed_numeric_result`, `RTP:real_power_result`, `RTP:real_mixed_comparison_result`, `RTP:real_mixed_equality_result` | Mixed arithmetic, power, comparison, and equality fixtures now pin the real-family promotion rule across the operators that admit both integral and real operands. |
| 105 | `[2.4.4] Implicit integral-to-real conversion rejects x and z` | done | `RTN:implicit_real_promotion_rejects_x_bits_in_addition`, `RTN:implicit_real_promotion_rejects_z_bits_in_comparison` | Rich-type runtime negatives now cover mixed arithmetic and comparison with integral `x`/`z` operands, proving implicit promotion to `real` fails instead of fabricating a real value. |
| 106 | `[2.4.4] No implicit real/string cross-conversion` | done | `RTN:implicit_real_string_numeric_mixture_is_invalid`, `RTN:implicit_real_string_equality_mixture_is_invalid` | Verified: direct rich-type negatives now reject both numeric and equality operators that would otherwise require implicit conversion between `real` and `string`. |
| 107 | `[2.4.5] String common-type restrictions` | done | `RTP:string_conditional_result`, `RTP:string_equality_exact`, `RTN:invalid_string_numeric_add`, `RTN:invalid_string_boolean_context`, `RTN:invalid_string_conditional_mixed_integral_arms`, `RTN:invalid_string_conditional_mixed_real_arms`, `RTN:invalid_conditional_mixed_integral_string_arms`, `RTN:invalid_conditional_mixed_real_string_arms` | Direct rich-type negatives now reject mixed string/integral and string/real ternary arms in both result-arm orders, while existing fixtures keep same-type string ternaries and string equality pinned. |
| 108 | `[2.4.6] Result-type guidance matrix` | done | `IBP:logical_result_is_scalar_four_state_even_for_two_state_operands`, `IBP:integral_common_type_default_addition_uses_max_width_sign_and_state`, `RTP:real_mixed_numeric_result`, `RTP:conditional_result_type_uses_arm_common_type_not_real_condition` | Direct matrix fixtures now pin all four result-typing buckets: logical operators return 1-bit 4-state integrals, integral arithmetic follows common-type width/sign/state, mixed integral/real arithmetic returns `real`, and `?:` keeps its real condition self-determined while resolving type from the arms only. |

### Selection, Operators, and Expression Evaluation (`109`-`138`)

| ID | Checklist point | Status | Current tests | Notes |
| --- | --- | --- | --- | --- |
| 109 | `[2.5.3] Selection works only on packed integral values, including derived packed values` | done | `IBP:dynamic_bit_select_valid`, `IBP:part_select_and_indexed_part_select`, `IBP:concatenation_select_uses_derived_width`, `IBP:replication_select_indexes_replicated_bits` | Verified: the existing fixture set covers selection from a packed signal plus derived packed concatenation and replication values, matching the checklist verification surface. |
| 110 | `[2.5.3] Selection rejects scalar integral, real, and string` | done | `IBN:invalid_selection_base_scalar_integer_like`, `IBN:invalid_part_select_base_scalar_integer_like`, `RTN:invalid_bit_select_base_real`, `RTN:invalid_part_select_base_real`, `RTN:invalid_bit_select_base_string`, `RTN:invalid_part_select_base_string` | Direct negative fixtures now cover both bit-select and part-select rejection for scalar integral, real, and string operands. |
| 111 | `[2.5.3] Selection bound and runtime rules matrix` | done | `IBP:dynamic_bit_select_valid`, `IBP:part_select_and_indexed_part_select`, `IBP:indexed_down_part_select_valid`, `IBN:invalid_const_part_select_bound`, `IBN:invalid_const_part_select_lsb`, `IBN:invalid_const_indexed_width`, `IBN:invalid_const_indexed_down_width`, `IBN:indexed_part_select_width_must_be_positive` | Direct fixtures now cover self-determined bit-select indices, fixed part-select with constant-only bounds, and indexed `+:` / `-:` forms with runtime bases plus const-required and positive-width rejection on the width position. |
| 112 | `[2.5.3] Selection result type and identity rules` | done | `IBP:dynamic_bit_select_valid`, `IBP:concatenation_select_uses_derived_width`, `IBP:integer_like_concat_part_select_drops_identity`, `IBP:enum_concat_full_slice_drops_identity` | Direct selection fixtures now pin width-1 bit-select and sized part-select results as plain unsigned bit-vectors, and single-operand concatenation bases prove selection drops integer-like and enum identity/labels even when the source value started scalar. |
| 113 | `[2.5.3] Invalid bit-select behavior matrix` | done | `IBP:dynamic_bit_select_out_of_range`, `IBP:dynamic_bit_select_unknown_index`, `IBP:dynamic_bit_select_out_of_range_two_state` | Direct fixtures now pin the invalid bit-select split: 4-state sources return `x` for out-of-range or unknown indices, while a matching 2-state out-of-range case returns `0`. |
| 114 | `[2.5.3] Invalid part-select behavior matrix` | done | `IBP:part_select_fully_out_of_range_four_state_source`, `IBP:part_select_fully_out_of_range_two_state_source`, `IBP:part_select_partially_out_of_range_four_state_source`, `IBP:part_select_partially_out_of_range_two_state_source`, `IBP:indexed_part_select_unknown_base_four_state_source`, `IBP:indexed_part_select_unknown_base_two_state_source`, `IBP:indexed_part_select_negative_base_four_state_source`, `IBP:indexed_part_select_negative_base_two_state_source` | Direct fixtures now cover fully out-of-range, partially out-of-range, unknown indexed-base, and signed negative indexed-base reads against both 4-state and 2-state packed sources, with invalid results typed as 4-state and yielding all-`x` or mixed bits plus `x` as required. |
| 115 | `[2.5.4] Logical operator acceptance, result type, and short-circuit matrix` | done | `IBP:boolean_context_not_reuses_integral_truth_value`, `IBP:boolean_context_and_reuses_integral_truth_value`, `IBP:boolean_context_or_reuses_integral_truth_value`, `IBP:boolean_context_integral_x_is_unknown`, `IBP:boolean_context_integral_z_is_unknown`, `IBP:logical_result_is_scalar_four_state_even_for_two_state_operands`, `event_runtime_short_circuit_holds`, `integral_boolean_short_circuit_preservation_holds`, `RTP:real_truthiness_not`, `RTP:real_logical_and_zero`, `RTP:real_logical_or_value`, `RTN:invalid_string_boolean_context` | Verified: existing integral and real fixtures cover `!`, `&&`, and `\|\|`, including ambiguous-integral truth collapsing to `x`, 1-bit unsigned 4-state result typing, string rejection, and the existing code-only short-circuit traps for both logical and event-eval surfaces. |
| 116 | `[2.5.5] Bitwise operator acceptance and common-type rules` | done | `IBP:bitwise_and_common_type_uses_mixed_width_sign_and_state`, `IBP:bitwise_xor_common_type_keeps_signed_two_state_when_both_operands_are_signed`, `IBP:bitwise_xnor_accepts_tilde_caret_spelling_and_uses_common_type`, `IBP:bitwise_xnor_accepts_caret_tilde_spelling_and_uses_common_type`, `IBP:bitwise_not_on_enum_operand_preserves_width_and_uses_underlying_bits`, `IBP:enum_common_type_ignores_labels_for_bitwise_or`, `RTN:invalid_bitwise_and_lhs_real`, `RTN:invalid_bitwise_or_rhs_string`, `RTN:invalid_bitwise_not_operand_real` | Direct fixtures now cover binary common-type coercion across mixed width/sign/state combinations, both XNOR spellings, unary `~` width preservation, enum bit-vector participation, and semantic rejection of non-integral operands for both unary and binary bitwise operators. |
| 117 | `[2.5.5] Bitwise truth-table controlling-value cases` | done | `IBP:bitwise_and_truth_table_preserves_controlling_zero_against_unknowns`, `IBP:bitwise_or_truth_table_preserves_controlling_one_against_unknowns`, `IBP:bitwise_xor_truth_table_treats_unknown_and_high_impedance_as_ambiguous`, `IBP:bitwise_xnor_tilde_caret_truth_table_matches_four_state_semantics`, `IBP:bitwise_xnor_caret_tilde_truth_table_matches_four_state_semantics` | Direct 16-lane fixtures enumerate the full `0/1/x/z` pair matrix for `&`, `|`, `^`, `~^`, and `^~`, including the controlling cases `0 & x -> 0` and `1 | x -> 1`. |
| 118 | `[2.5.6] Reduction operator rules matrix` | done | `IBP:reduction_and_two_state_all_ones`, `IBP:reduction_nand_x_bits_propagate_to_x`, `IBP:reduction_or_four_state_all_zero`, `IBP:reduction_nor_z_bits_propagate_to_x`, `IBP:reduction_xor_signed_operand_is_self_determined`, `IBP:reduction_xnor_accepts_tilde_caret_spelling`, `IBP:reduction_xnor_unknown_bits_propagate_to_x`, `RTN:invalid_reduction_and_operand_real`, `RTN:invalid_reduction_xor_operand_string` | Direct fixtures now cover all unary reduction operators, including both XNOR spellings, across 2-state and 4-state integral operands; they pin the 1-bit unsigned 4-state result type, prove signed operands stay self-determined, independently lock both `x` and `z` propagation to `x`, and reject non-integral operands. |
| 119 | `[2.5.7] Exponentiation acceptance and result-family rules` | done | `RTP:real_power_result`, `RTP:real_power_real_operands_result`, `IBP:power_integral_common_type_preserves_signed_result_when_both_operands_are_signed`, `IBP:power_integral_common_type_uses_max_width_and_unsigned_sign`, `IBP:power_exponent_remains_self_determined_against_unsigned_base` | Direct fixtures now cover integral/integral, integral/real, and real/real exponentiation; they pin signed versus unsigned integral result families through common-type resolution and prove the exponent operand stays self-determined even when the result family is unsigned. |
| 120 | `[2.5.7] Exponentiation special-case matrix` | done | `IBP:zero_to_zero_power_returns_one`, `IBP:zero_to_negative_power_returns_unknown`, `IBP:nonzero_to_negative_power_rounds_to_zero` | Direct fixtures now lock both exponent-zero and negative-exponent branches, including the zero-base edge case where `0 ** 0` still yields exactly `1`. |
| 121 | `[2.5.8] Arithmetic supported-operator family` | done | `IBP:arithmetic_unary_plus_accepts_integral_operand`, `IBP:arithmetic_unary_minus_accepts_integral_operand`, `IBP:precedence_and_associativity_core`, `IBP:arithmetic_binary_minus_accepts_integral_operands`, `IBP:arithmetic_multiplication_accepts_integral_operands`, `IBP:signed_division_preserves_twos_complement_result`, `IBP:signed_modulo_preserves_twos_complement_result` | Direct logical-eval fixtures now exercise unary `+`, unary `-`, binary `+`, `-`, `*`, `/`, and `%` with one representative accepted case for each operator. |
| 122 | `[2.5.8] Arithmetic common-type and promotion rules` | done | `IBP:integral_common_type_default_addition_uses_max_width_sign_and_state`, `IBP:integral_common_type_sign_state_signed_two_state_addition`, `IBP:integral_common_type_sign_state_signed_four_state_addition`, `IBP:integral_common_type_sign_state_unsigned_two_state_addition`, `IBP:integral_common_type_sign_state_unsigned_four_state_addition`, `IBP:arithmetic_pure_integral_common_type_uses_integral_result_family`, `RTP:real_mixed_numeric_result`, `RTP:arithmetic_mixed_numeric_promotion_real_rhs`, `RTP:arithmetic_mixed_numeric_promotion_real_lhs` | Direct fixtures now pin pure-integral arithmetic to the resolved integral common type across width/sign/state combinations and compare the same signed `-1` plus `3` values against mixed real arithmetic in both operand orders, proving promotion to `real`. |
| 123 | `[2.5.8] Integral arithmetic x/z contagion and zero-divisor matrix` | done | `IBP:arithmetic_addition_x_operand_yields_all_x`, `IBP:arithmetic_subtraction_z_operand_yields_all_x`, `IBP:arithmetic_multiplication_x_operand_yields_all_x`, `IBP:arithmetic_division_z_operand_yields_all_x`, `IBP:arithmetic_division_by_zero_yields_all_x`, `IBP:arithmetic_modulo_x_operand_yields_all_x`, `IBP:arithmetic_modulo_by_zero_yields_all_x` | Direct fixtures now cover x/z contagion across every binary integral arithmetic operator and separately pin divide-by-zero plus modulo-by-zero as all-`x`. |
| 124 | `[2.5.8] Integral division truncates toward zero` | done | `IBP:signed_division_truncates_positive_non_integer_quotient`, `IBP:signed_division_truncates_negative_lhs_toward_zero`, `IBP:signed_division_truncates_negative_rhs_toward_zero`, `IBP:signed_division_truncates_double_negative_toward_zero` | Direct logical-eval fixtures now cover non-integer integral division across all sign combinations and pin the quotient as truncating toward zero. |
| 125 | `[2.5.9] Shift acceptance and width/count rules` | done | `IBP:shift_left_result_width_matches_left_operand_width`, `IBP:shift_right_result_width_matches_wide_left_operand_width`, `IBP:shift_count_uses_rhs_self_determined_unsigned_value`, `RTN:invalid_shift_lhs_real`, `RTN:invalid_shift_rhs_string` | Direct fixtures now cover narrow and wide left operands preserving result width, prove signed rhs counts stay self-determined and are interpreted as unsigned, and reject both real and string shift operands. |
| 126 | `[2.5.9] Shift fill-rule matrix` | done | `IBP:shift_left_zero_fills_vacated_lsb_bits`, `IBP:shift_right_zero_fills_vacated_msb_bits`, `IBP:signed_shift_right_zero_fills_vacated_msb_bits`, `IBP:arithmetic_left_shift_zero_fills_vacated_lsb_bits`, `IBP:arithmetic_right_shift_unsigned_zero_fills_vacated_msb_bits`, `IBP:arithmetic_right_shift_signed_sign_fills_vacated_msb_bits` | Direct logical-eval fixtures now cover all four shift operators, proving `<<`, `>>`, and `<<<` zero-fill vacated bits, including signed `>>`, while `>>>` zero-fills unsigned results and sign-fills signed results. |
| 127 | `[2.5.9] Unknown shift counts yield all-x` | done | `IBP:shift_left_unknown_count_yields_all_x`, `IBP:shift_right_unknown_count_yields_all_x`, `IBP:arithmetic_left_shift_unknown_count_yields_all_x`, `IBP:arithmetic_right_shift_unknown_count_yields_all_x` | Direct logical-eval fixtures now cover all four shift operators with rhs literals containing `x` or `z`, and each result becomes all-`x` while preserving the left operand's width and signedness. |
| 128 | `[2.5.10] Comparison acceptance, promotion, and result rules` | done | `IBP:integral_common_type_default_comparison_uses_converted_operands`, `IBP:integral_comparison_with_x_bit_yields_x`, `IBP:integral_comparison_with_x_bit_on_lhs_yields_x`, `IBP:integral_comparison_with_z_bit_yields_x`, `IBP:integral_comparison_with_z_bit_on_rhs_yields_x`, `RTP:real_comparison_accepts_real_operands`, `RTP:comparison_mixed_numeric_promotion_real_rhs`, `RTP:comparison_mixed_numeric_promotion_real_lhs`, `RTN:invalid_comparison_rhs_string`, `RTN:invalid_comparison_lhs_string` | Direct fixtures now cover integral-only, real-only, and mixed integral/real comparisons across all four comparison operators, pin implicit promotion to `real` in both operand orders with boundary-sensitive cases, reject unsupported string operands on either side, and prove the result stays a 1-bit unsigned 4-state value while integral `x`/`z` input on either side yields `x`. |
| 129 | `[2.5.11] Equality operator acceptance matrix` | done | `IBP:integral_common_type_default_equality_preserves_four_state_operands`, `IBP:integral_not_equal_accepts_integral_operands`, `RTP:real_mixed_equality_result`, `RTP:real_mixed_equality_accepts_real_lhs`, `RTP:real_equality_accepts_real_operands`, `RTP:real_mixed_inequality_accepts_mixed_numeric_operands`, `RTP:real_mixed_inequality_accepts_real_lhs`, `RTP:real_not_equal_accepts_real_operands`, `RTP:string_equality_exact`, `RTP:string_not_equal_accepts_string_operands`, `IBP:case_equality_distinguishes_unknown_bits`, `IBP:wildcard_and_case_equality`, `IBP:wildcard_equality_matches_unknown_bits`, `IBP:wildcard_inequality_accepts_integral_operands`, `RTN:implicit_real_string_equality_mixture_is_invalid`, `RTN:implicit_real_string_equality_mixture_is_invalid_reversed`, `RTN:implicit_real_string_inequality_mixture_is_invalid`, `RTN:implicit_real_string_inequality_mixture_is_invalid_reversed`, `RTN:implicit_integral_string_equality_mixture_is_invalid`, `RTN:implicit_integral_string_equality_mixture_is_invalid_reversed`, `RTN:implicit_integral_string_inequality_mixture_is_invalid`, `RTN:implicit_integral_string_inequality_mixture_is_invalid_reversed`, `RTN:case_equality_rejects_real_operands`, `RTN:case_equality_rejects_string_operands`, `RTN:case_inequality_rejects_real_operands`, `RTN:case_inequality_rejects_string_operands`, `RTN:case_equality_rejects_real_rhs`, `RTN:case_equality_rejects_string_rhs`, `RTN:case_inequality_rejects_real_rhs`, `RTN:case_inequality_rejects_string_rhs`, `RTN:wildcard_equality_rejects_real_operands`, `RTN:wildcard_equality_rejects_string_operands`, `RTN:wildcard_inequality_rejects_real_operands`, `RTN:wildcard_inequality_rejects_string_operands`, `RTN:wildcard_equality_rejects_real_rhs`, `RTN:wildcard_equality_rejects_string_rhs`, `RTN:wildcard_inequality_rejects_real_rhs`, `RTN:wildcard_inequality_rejects_string_rhs` | Direct positive and negative fixtures now cover `==`, `!=`, `===`, `!==`, `==?`, and `!=?`, proving `==`/`!=` accept integral, real-only, mixed real/integral in both operand orders, and string/string operands while mixed real/string and integral/string orderings fail for both spellings and case/wildcard equality reject non-integral operands on either side for every spelling. |
| 130 | `[2.5.11] Integral equality result semantics matrix` | done | `RTP:real_mixed_equality_result`, `IBP:integral_common_type_default_equality_preserves_four_state_operands`, `IBP:integral_not_equal_with_unknown_bits_yields_x`, `IBP:case_equality_distinguishes_unknown_bits`, `IBP:case_inequality_distinguishes_unknown_bits`, `IBP:case_equality_matches_identical_unknown_bits`, `IBP:case_inequality_matches_identical_unknown_bits`, `IBP:wildcard_equality_matches_unknown_bits`, `IBP:wildcard_equality_matches_high_impedance_bits`, `IBP:wildcard_equality_lhs_unknown_is_not_wildcard`, `IBP:wildcard_inequality_accepts_integral_operands`, `IBP:wildcard_inequality_unknown_bits_match_returns_zero`, `IBP:wildcard_inequality_high_impedance_bits_match_returns_zero`, `IBP:wildcard_inequality_lhs_high_impedance_is_not_wildcard` | Mixed integral/real promotion stays covered in the rich-types suite, while direct integral fixtures now pin `==`/`!=` unknown results, both known outcomes for `===`/`!==`, and `==?`/`!=?` wildcarding only on rhs `x/z` across match, mismatch, and lhs-unknown cases. |
| 131 | `[2.5.11] String equality exact-match semantics matrix` | done | `RTP:string_equality_exact`, `RTP:string_not_equal_accepts_string_operands`, `RTP:string_not_equal_exact_match_returns_zero`, `RTP:string_equality_different_contents`, `RTP:string_equality_different_lengths`, `RTN:implicit_real_string_equality_mixture_is_invalid`, `RTN:implicit_real_string_equality_mixture_is_invalid_reversed`, `RTN:implicit_integral_string_equality_mixture_is_invalid`, `RTN:implicit_integral_string_equality_mixture_is_invalid_reversed`, `RTN:implicit_real_string_inequality_mixture_is_invalid`, `RTN:implicit_real_string_inequality_mixture_is_invalid_reversed`, `RTN:implicit_integral_string_inequality_mixture_is_invalid`, `RTN:implicit_integral_string_inequality_mixture_is_invalid_reversed` | Direct rich-type fixtures now cover exact and non-exact `==`/`!=` results across equal strings, different contents, and different lengths, while rich-type negatives reject mixed string/real and string/integral equality in both operand orders. |
| 132 | `[2.5.12] Conditional operator condition and arm-resolution rules` | done | `IBP:conditional_multibit_condition_nonzero_selects_true_arm`, `IBP:conditional_multibit_condition_zero_selects_false_arm`, `RTP:real_condition_in_ternary`, `IBP:enum_conditional_preserves_identity`, `RTP:string_conditional_result` | Direct fixtures now cover width-sensitive integral and real conditions using boolean-context truthiness, while mismatched integral arms, same-enum arms, and string arms pin that the result type comes only from the two arms. |
| 133 | `[2.5.12] Conditional x/z merge rules matrix` | done | `IBP:conditional_unknown_condition_merges_bits`, `RTP:enum_conditional_merges_unknown_identity`, `RTP:enum_conditional_z_merge_preserves_exact_label`, `IBP:enum_conditional_preserves_identity` | Verified the existing integral and unlabeled same-enum merge cases, then added a `z`-condition rich-type fixture to prove same-enum ternaries keep enum identity and retain the declared label when the merged bits still exactly match that label. |
| 134 | `[2.5.13] inside accepted narrowed form and item kinds` | done | `IBP:concat_replication_inside_conditional`, `IBP:inside_returns_unknown_for_unknown_membership`, `IBP:iff_inside_and_concatenation` | Verified existing fixtures already cover the narrowed accepted `inside` surface: integral lhs values, both integral-expression and inclusive-range set items, and use in both direct logical evaluation and `iff` guards. |
| 135 | `[2.5.13] inside matching semantics and x result rule` | done | `IBP:inside_wildcard_membership_matches`, `IBP:inside_returns_zero_for_definite_miss`, `IBP:inside_returns_unknown_for_unknown_membership`, `IBP:concat_replication_inside_conditional`, `IBP:wildcard_and_case_equality` | Direct `inside` fixtures now isolate the full result split: wildcard membership returns `1`, definite misses return `0`, and ambiguous no-match cases return `x`, while `==?` coverage keeps the shared matching rule explicit. |
| 136 | `[2.5.13] Unsupported advanced inside forms matrix` | done | `IBN:inside_rejects_open_range_dollar_bound`, `IBN:inside_rejects_plus_minus_tolerance_item`, `IBN:inside_rejects_plus_percent_minus_tolerance_item`, `IBN:inside_rejects_unpacked_array_assignment_pattern_item`, `IBN:inside_rejects_scan_order_dependent_aggregate_form` | Direct logical negatives now attempt representative open-range `$`, `+/-`, `+%-`, unpacked-array aggregate, and scan-order-sensitive aggregate `inside` forms. The current narrowed surface rejects the `$` bound via generic unknown-signal binding and rejects both aggregate-style forms syntactically before unpacked-array or scan-order semantics become reachable. |
| 137 | `[2.5.14] Concatenation and replication acceptance and identity rules` | done | `IBP:concat_replication_inside_conditional`, `IBP:concatenation_select_uses_derived_width`, `IBP:replication_select_indexes_replicated_bits`, `IBP:integer_like_concat_part_select_drops_identity`, `IBP:enum_concat_full_slice_drops_identity`, `IBP:integer_like_concatenation_drops_identity`, `IBP:enum_concatenation_drops_identity`, `IBP:enum_replication_drops_identity`, `IBP:integer_like_replication_drops_identity`, `IBP:integer_like_replication_part_select_drops_identity`, `IBP:enum_replication_full_slice_drops_identity`, `RTN:invalid_raw_event_concatenation`, `RTN:invalid_raw_event_replication`, `RTN:invalid_real_concatenation_operand`, `RTN:invalid_string_concatenation_operand`, `RTN:invalid_string_replication_operand`, `RTN:invalid_real_replication_operand` | Direct fixtures now cover concatenation and replication built from ordinary integral, integer-like, and enum operands; bare concat and replication forms already collapse integer-like and enum values to plain unsigned bit-vectors, later selection from the derived result remains legal, and rich-type negatives keep raw event, real, and string operands out of both concatenation and replication. |
| 138 | `[2.5.14] Concatenation and replication width/state/value rules matrix` | done | `IBP:concatenation_width_sum_stays_two_state`, `IBP:concatenation_promotes_to_four_state_and_preserves_xz`, `IBP:replication_width_scales_two_state_operand`, `IBP:replication_preserves_xz_bits_in_four_state_operand`, `IBN:invalid_const_replication_multiplier`, `IBN:replication_rejects_negative_multiplier`, `IBN:replication_rejects_zero_multiplier`, `IBN:concat_rejects_unsized_literal_operand` | Direct logical fixtures now pin concatenation width summation, replication width scaling, 2-state versus 4-state result state, and `x`/`z` preservation, while negative fixtures reject unsized concat literals plus zero, non-constant, and negative replication multipliers. |

### Precedence and Grammar (`139`-`168`)

| ID | Checklist point | Status | Current tests | Notes |
| --- | --- | --- | --- | --- |
| 139 | `[2.6] Primary and postfix forms bind tighter than infix operators` | done | `IBP:precedence_and_associativity_core`, `IBP:parenthesized_primary_binds_before_equality`, `IBP:integral_cast_primary_binds_before_addition`, `IBP:concatenation_primary_binds_before_equality`, `IBP:replication_primary_binds_before_equality`, `RTP:operand_type_cast_real_primary_binds_before_addition`, `RTP:enum_label_primary_binds_before_equality`, `RTP:rich_iff_with_triggered_real_and_string` | Verified the existing selection, cast, and `.triggered` precedence fixtures, then added direct parenthesized, concatenation, replication, and enum-label equality cases so each supported primary/postfix family now has an infix-adjacent precedence check before the row is marked done. |
| 140 | `[2.6] Concatenation and replication behave as primary expressions in mixed expressions` | done | `IBP:concatenation_primary_participates_in_shift_without_parentheses`, `IBP:replication_primary_participates_in_addition_without_parentheses`, `IBP:concat_replication_inside_conditional`, `IBP:concatenation_select_uses_derived_width`, `IBP:replication_select_indexes_replicated_bits` | Direct shift and arithmetic fixtures now use values that distinguish the intended grouping from plausible mis-groupings, proving `{...}` and `{N{...}}` act as whole primaries without extra parentheses. |
| 141 | `[2.6] .triggered has postfix binding like selection` | done | `RTP:triggered_postfix_binds_before_unary_not`, `RTP:triggered_postfix_unary_not_matches_parenthesized_operand`, `RTP:triggered_postfix_binds_before_equality`, `RTP:triggered_postfix_equality_matches_parenthesized_operand` | Direct rich-type fixture pairs now compare `!ev.triggered` with `!(ev.triggered)` and `ev.triggered == 1'b1` with `(ev.triggered) == 1'b1`, isolating `.triggered` as a postfix form before unary and infix neighbors. |
| 142 | `[2.6] Postfix forms chain left-to-right` | done | `IBP:concatenation_select_uses_derived_width`, `IBP:concatenation_select_chain_associates_left_to_right`, `IBP:replication_select_indexes_replicated_bits` | Added a direct chained selection fixture on a concatenation result so the suite now exercises repeated postfix parsing and evaluation with unparenthesized `expr[a][b]` left-to-right. |
| 143 | `[2.6] Prefix unary operators bind to the following operand` | done | `IBP:arithmetic_unary_minus_binds_before_addition`, `IBP:arithmetic_unary_minus_addition_matches_parenthesized_operand`, `IBP:logical_not_binds_before_addition`, `IBP:logical_not_addition_matches_parenthesized_operand`, `IBP:bitwise_not_binds_before_addition`, `IBP:bitwise_not_addition_matches_parenthesized_operand`, `IBP:reduction_and_binds_before_addition`, `IBP:reduction_and_addition_matches_parenthesized_operand` | Direct unary-vs-addition fixture pairs now compare the unparenthesized and explicitly parenthesized forms for arithmetic `-`, logical `!`, bitwise `~`, and reduction `&`, isolating prefix binding across representative unary families. |
| 144 | `[2.6] Exponentiation precedence relative to unary and multiplicative operators` | done | `IBP:power_binds_after_unary_minus`, `IBP:power_unary_minus_matches_parenthesized_operand`, `IBP:power_binds_before_multiplication`, `IBP:power_multiplication_matches_parenthesized_operand` | Direct logical-eval fixture pairs now prove `**` groups below prefix unary `-` and above `*` by matching the unparenthesized forms against the corresponding explicit-parentheses spellings. |
| 145 | `[2.6] Multiplicative, additive, and shift precedence chain` | done | `IBP:precedence_and_associativity_core`, `IBP:multiplicative_additive_shift_precedence_chain`, `IBP:multiplicative_additive_shift_matches_parenthesized_chain` | Direct logical-eval fixture pair now proves `*` binds above `+`, and the combined additive result then binds above `<<` by matching the unparenthesized form against the explicit-parentheses spelling. |
| 146 | `[2.6] Relational and inside precedence relative to shifts and equality` | done | `IBP:shift_relational_equality_precedence_chain`, `IBP:shift_relational_equality_matches_parenthesized_chain`, `IBP:shift_inside_equality_precedence_chain`, `IBP:shift_inside_equality_matches_parenthesized_chain` | Direct logical-eval fixture pairs now prove both relational operators and `inside` sit below shifts and above equality by matching each unparenthesized form against the explicit-parentheses spelling. |
| 147 | `[2.6] Equality and bitwise precedence chain` | done | `IBP:equality_bitwise_precedence_chain`, `IBP:equality_bitwise_matches_parenthesized_chain`, `IBP:equality_bitwise_xnor_precedence_chain`, `IBP:equality_bitwise_xnor_matches_parenthesized_chain` | Direct logical-eval fixture pairs now prove equality binds above bitwise `&`, bitwise `&` binds above `^`/`~^`, and those bitwise XOR/XNOR forms bind above `|` by matching each unparenthesized chain against the explicit-parentheses spelling. |
| 148 | `[2.6] Logical and conditional precedence chain` | done | `IBP:logical_and_or_conditional_precedence_chain`, `IBP:logical_and_or_conditional_matches_parenthesized_chain` | Direct logical-eval fixture pair now proves `&&` binds above `||`, and the full logical chain binds above `?:`, by matching the unparenthesized form against the explicit-parentheses spelling. |
| 149 | `[2.6] Binary infix operators associate left-to-right` | done | `IBP:binary_minus_chain_associates_left_to_right`, `IBP:binary_minus_chain_matches_left_parenthesized_grouping`, `IBP:shift_right_chain_associates_left_to_right`, `IBP:shift_right_chain_matches_left_parenthesized_grouping` | Added subtraction and right-shift chain fixture pairs whose chosen operands distinguish left association from the right-associated alternatives, then matched each unparenthesized form against the explicit left-grouped spelling across two binary tiers. |
| 150 | `[2.6] Conditional operator associates right-to-left` | done | `IBP:conditional_chain_associates_right_to_left`, `IBP:conditional_chain_matches_right_parenthesized_grouping`, `IBP:conditional_chain_left_parenthesized_grouping_differs` | Added a nested ternary fixture pair plus an explicit left-grouped contrast case whose operands make right association produce `4'd0` while the left-grouped alternative yields `4'd9`, so the suite now proves unparenthesized `?:` binds right-to-left by matching the explicit right-grouped spelling. |
| 151 | `[2.6] Casts and enum-label primaries bind as primary expressions` | done | `RTP:operand_type_cast_string_primary_binds_before_equality`, `RTP:operand_type_cast_string_equality_matches_parenthesized_operand`, `RTP:enum_label_primary_binds_before_equality` | Added a direct operand-type-cast equality fixture pair that matches the unparenthesized form against the explicit-parenthesized spelling; together with the existing enum-label equality fixture, the row now isolates both cast and enum-label primary binding before infix equality. |
| 152 | `[2.7] Start symbol and ternary grammar` | done | `IBP:bare_logical_or_is_valid_top_level_expr`, `IBP:nested_ternary_grammar_accepts_recursive_false_branch`, `logical_parser_accepts_integral_boolean_surface_sample`, `logical_parser_accepts_rich_type_surface_sample` | Added direct logical-eval fixtures for a bare top-level logical-or expression and a value-distinguishing unparenthesized nested ternary, so the suite now exercises the `expr ::= conditional_expr` entry point and proves the right-recursive ternary grammar directly. |
| 153 | `[2.7] Logical-chain grammar for || and &&` | done | `IBP:logical_or_chain_accepts_repetition`, `IBP:logical_and_chain_accepts_repetition`, `ERP:iff_bounded_ops_and_literals`, `event_runtime_short_circuit_holds`, `RTP:rich_iff_with_triggered_real_and_string` | Direct logical-eval fixtures now isolate repeated `||` and `&&` chains, while the existing event and rich-type cases keep chained logical forms exercised inside `iff` guards. |
| 154 | `[2.7] Bitwise, equality, and relational chain grammar` | done | `IBP:bitwise_or_chain_accepts_repetition`, `IBP:bitwise_xor_chain_accepts_repetition`, `IBP:bitwise_and_chain_accepts_repetition`, `IBP:equality_chain_accepts_repetition`, `IBP:relational_inside_chain_accepts_repetition` | Direct logical-eval fixtures now isolate repeated bitwise `|`, mixed-spelling xor/xnor, bitwise `&`, wildcard/case-equality, and mixed relational-`inside` chains. |
| 155 | `[2.7] Shift, additive, multiplicative, and power chain grammar` | done | `IBP:shift_chain_accepts_repetition`, `IBP:additive_chain_accepts_repetition`, `IBP:multiplicative_chain_accepts_repetition`, `IBP:power_chain_accepts_repetition` | Direct logical-eval fixtures now isolate repeated shift, additive, multiplicative, and exponentiation chains, so each grammar level is exercised with an unparenthesized repeated operator sequence before the row is marked done. |
| 156 | `[2.7] Recursive unary grammar` | done | `IBP:recursive_unary_chain_accepts_repetition`, `IBP:recursive_unary_chain_matches_nested_parentheses` | Direct logical-eval fixture pair now accepts a three-deep mixed unary chain and matches the unparenthesized spelling against explicit recursive nesting. |
| 157 | `[2.7] Repeated postfix grammar` | done | `IBP:nested_selection_chain_accepts_repeated_postfix_suffixes`, `IBP:concatenation_select_chain_associates_left_to_right` | Added a direct nested-selection fixture on a plain packed operand, and the existing concatenation chain keeps the derived-value variant covered, so repeated postfix suffixes are now exercised in sequence. |
| 158 | `[2.7] Postfix suffix grammar matrix` | done | `RTP:postfix_suffix_grammar_matrix_accepts_all_documented_forms` | Direct rich-type coverage now exercises `[expr]`, `[constant_expr : constant_expr]`, `[expr +: constant_expr]`, `[expr -: constant_expr]`, and `.triggered` in one fixture. |
| 159 | `[2.7] Primary-expression grammar matrix` | done | `logical_parser_accepts_integral_boolean_surface_sample`, `logical_parser_accepts_rich_type_surface_sample`, `IBP:concat_replication_inside_conditional`, `RTP:enum_label_reference` | Existing parser samples and fixture-backed evaluations collectively cover operand references, literals, parenthesized expressions, casts, enum-label expressions, concatenations, and replications, so the row is now verified complete. |
| 160 | `[2.7] Concatenation and replication grammar details` | done | `IBP:concatenation_grammar_accepts_multiple_elements`, `IBP:replication_grammar_accepts_constant_expr_multiplier`, `IBN:invalid_const_replication_multiplier` | Added direct fixture coverage for multi-element concatenation and a non-literal constant-expression replication multiplier, while the existing negative case continues to prove replication rejects non-constant multipliers. |
| 161 | `[2.7] inside set grammar details` | done | `IBP:concat_replication_inside_conditional`, `IBP:inside_returns_unknown_for_unknown_membership`, `IBP:iff_inside_and_concatenation` | Existing fixtures already verify mixed `inside` sets: one logical case combines a range item with a plain expression item, while the logical and event cases keep each form accepted independently, so the documented grammar shape is fully exercised. |
| 162 | `[2.7] Literal grammar matrix` | done | `RTP:literal_grammar_matrix`, `logical_parser_accepts_integral_boolean_surface_sample`, `lex_logical_expr_accepts_real_and_string_literals` | Direct rich-type coverage now parses and evaluates the documented integral, real, and string literal spellings in one fixture, while the existing parser and lexer regressions keep the broader surface smoke-tested. |
| 163 | `[2.7] type_reference grammar matrix` | done | `IBP:integral_cast_bit_logic_variants`, `IBP:integral_cast_integer_like_target`, `RTP:real_cast_target_family`, `RTP:string_identity_cast_literal`, `RTP:signed_cast_accepts_enum_operand`, `IBP:integral_cast_unsigned_target_family`, `RTP:operand_type_cast_real` | Verified: existing fixtures already parse accepted casts for bit-vector, integer-like, `real`, `string`, `signed`, `unsigned`, and recovered `type(...)` targets. |
| 164 | `[2.7] bit_vector_type grammar details` | done | `IBP:integral_cast_bit_logic_variants`, `IBP:integral_cast_scalar_bit_target`, `IBP:integral_cast_scalar_logic_target`, `IBP:integral_cast_scalar_bit_width1_target`, `IBP:integral_cast_scalar_logic_width1_target`, `IBP:integral_cast_scalar_bit_alias_concat_bare`, `IBP:integral_cast_scalar_bit_alias_concat_width1`, `IBP:integral_cast_scalar_logic_alias_concat_bare`, `IBP:integral_cast_scalar_logic_alias_concat_width1`, `IBP:integral_cast_signed_bit_unsigned_logic_targets`, `IBP:integral_cast_unsigned_bit_signed_logic_targets`, `IBP:integral_cast_signed_logic_unsigned_bit_targets`, `IBP:integral_cast_unsigned_logic_signed_bit_targets`, `IBN:invalid_cast_target_width` | Verified: existing fixtures already cover bare `bit`/`logic`, explicit `[1]` aliases, signed and unsigned `bit`/`logic` vector targets, and the invalid zero-width rejection. |
| 165 | `[2.7] integer_like_type grammar keyword matrix` | done | `IBP:integral_cast_integer_like_target`, `IBP:integral_cast_byte_keyword_target`, `IBP:integral_cast_shortint_keyword_target`, `IBP:integral_cast_longint_keyword_target`, `IBP:integral_cast_integer_keyword_target`, `IBP:integral_cast_time_keyword_target` | Direct logical-eval cast fixtures now sample all six documented integer-like target keywords, with 2-state and 4-state families both represented by their direct cast spellings. |
| 166 | `[2.7] Hierarchical operand references are part of the grammar surface` | done | `IBP:hierarchical_operand_reference_standalone` | Direct logical-eval coverage now binds and evaluates `top.cpu.data` as an ordinary section-2 operand reference. |
| 167 | `[2.7] .triggered is a reserved postfix form with semantic restrictions` | done | `lex_logical_expr_reserves_triggered_suffix`, `RTP:triggered_logical_primary`, `RTN:invalid_triggered_on_non_event`, `RTN:invalid_triggered_on_parenthesized_event`, `RTN:invalid_chained_triggered` | Added a direct negative fixture for `(ev).triggered`, so the row now covers the reserved lexer split, the valid raw-event form, and the semantic restriction that `.triggered` must apply directly to an event operand reference and not to parenthesized/chained variants. |
| 168 | `[2.7] Semantic validation for enum labels, constant expressions, and inclusive ranges` | done | `RTP:enum_label_reference`, `RTN:invalid_enum_label_on_non_enum`, `RTN:invalid_missing_enum_label`, `IBN:invalid_const_part_select_bound`, `IBP:inside_range_includes_lower_bound`, `IBP:inside_range_includes_upper_bound`, `IBP:inside_range_excludes_adjacent_value` | Added direct logical fixtures that isolate `inside` range bounds so the row now proves both inclusive endpoints and an adjacent miss alongside the existing enum-label and constant-expression semantic checks. |

### Late Checklist Additions (`169`-`214`)

| ID | Checklist point | Status | Current tests | Notes |
| --- | --- | --- | --- | --- |
| 169 | `[1] Event syntax rejects the outer @(...) wrapper` | done | `PN:outer_at_wrapper_rejects_posedge_form`, `PN:outer_at_wrapper_rejects_union_form`, `PN:outer_at_wrapper_rejects_wildcard_form`, `PP:edge_keywords`, `PP:or_union`, `PP:wildcard_any_tracked` | Added direct parse-negative fixtures for `@(posedge clk)`, `@(a or b)`, and `@(*)`, while the existing positive parse fixtures keep the corresponding inner event forms accepted without the outer wrapper. |
| 170 | `[2.3.13] .triggered follows ordinary integral cast semantics` | done | `RTP:triggered_cast_semantics_match_ordinary_bit` | Direct rich-type coverage now compares `.triggered` against an ordinary `bit` carrying the same `1` value across representative width-changing, state-domain-changing, and signedness-changing casts. |
| 171 | `[2.4.4] Implicit integral-to-real promotion uses the source's self-determined value` | done | `RTP:arithmetic_mixed_numeric_promotion_real_rhs`, `RTP:arithmetic_mixed_numeric_promotion_real_lhs`, `RTP:comparison_mixed_numeric_promotion_real_rhs`, `RTP:comparison_mixed_numeric_promotion_real_lhs` | Verified: existing rich-type fixtures already force `4'shf` to promote from its self-determined 4-bit signed value (`-1`) before real arithmetic/comparison, so the same cases cover both operand orders. |
| 172 | `[2.5.1] Non-whitelisted operator syntax is rejected` | done | `IBN:rejects_non_whitelisted_prefix_increment`, `IBN:rejects_non_whitelisted_postfix_increment`, `IBN:rejects_non_whitelisted_adjacent_increment_operands`, `IBN:rejects_non_whitelisted_prefix_decrement`, `IBN:rejects_non_whitelisted_postfix_decrement`, `IBN:rejects_non_whitelisted_adjacent_decrement_operands`, `IBN:rejects_non_whitelisted_binary_nand`, `IBN:rejects_non_whitelisted_binary_nor` | Direct logical negatives now reject contiguous `++`/`--` spellings in prefix, postfix, and adjacent-operand positions, while separate fixtures still reject binary `~&`/`~|` without disturbing the supported unary reduction forms. |
| 173 | `[2.5.3] Bit-select unknown indices use the invalid-index rule` | done | `IBP:dynamic_bit_select_unknown_index`, `IBP:dynamic_bit_select_unknown_index_two_state` | Added a direct 2-state bit-select fixture with a `z` index, so together with the existing 4-state `x`-index case the invalid-index rule is now pinned for both source domains. |
| 174 | `[2.6] Exponentiation chains associate left-to-right` | done | `IBP:power_chain_associates_left_to_right`, `IBP:power_chain_left_parenthesized_matches`, `IBP:power_chain_right_parenthesized_differs` | Added explicit `a ** b ** c`, `(a ** b) ** c`, and `a ** (b ** c)` fixtures so the unparenthesized chain is pinned to match the left-parenthesized form and differ from the right-parenthesized form. |
| 175 | `[2.5.3] Valid selection read semantics matrix` | done | `IBP:dynamic_bit_select_valid`, `IBP:part_select_valid_two_state_preserves_domain`, `IBP:indexed_up_part_select_valid_literal_base_preserves_two_state_domain`, `IBP:indexed_down_part_select_valid` | Direct logical-eval fixtures now pin valid `[]`, `[msb:lsb]`, `[base +: width]`, and `[base -: width]` reads, including 2-state part-select cases so selection results prove state-domain preservation instead of only boolean comparison success. |
| 176 | `[2.4.3] Mixed enum implicit conversions drop labels outside same-enum ternary` | done | `IBP:enum_conditional_mixed_integral_arm_drops_identity`, `IBP:enum_common_type_ignores_labels_for_bitwise_or` | Added a mixed enum/plain-integral ternary that selects a labeled enum sample but still yields an unlabeled plain integral result, and kept the mixed bitwise-or case so label-dropping is pinned beyond the same-enum `?:` special case. |
| 177 | `[2.7] type_reference rejects unsupported cast-target syntax` | done | `IBN:invalid_cast_target_event_keyword`, `IBN:invalid_cast_target_logic_packed_range`, `IBN:invalid_cast_target_recovered_type_expression` | Added direct logical parse negatives for unsupported `event'(...)`, packed-range `logic[7:0]'(...)`, and expression-bearing `type(a + idx)'(...)` cast targets, so unsupported `type_reference` spellings are now pinned to reject. |
| 178 | `[2.7] Only exact type(enum_operand_reference)::LABEL scope syntax is accepted` | done | `RTP:enum_label_reference`, `IBN:invalid_enum_label_pkg_scope`, `IBN:invalid_enum_label_named_type_scope`, `IBN:invalid_enum_label_recovered_type_expression_scope` | Direct parse negatives now reject nearby `pkg::ID`, `enum_t::LABEL`, and `type(expr)::LABEL` alternatives while the existing rich-type positive fixture keeps the exact documented form accepted. |
| 179 | `[2.7] Event-expression-only syntax is rejected in boolean expressions` | done | `IBN:rejects_event_wildcard_logical_expr`, `IBN:rejects_event_posedge_logical_expr`, `IBN:rejects_event_negedge_logical_expr`, `IBN:rejects_event_edge_logical_expr`, `IBN:rejects_event_or_union_logical_expr`, `IBN:rejects_event_comma_union_logical_expr`, `IBN:rejects_event_iff_logical_expr` | Added direct logical parse negatives for wildcard, edge-keyword, union, and `iff` event-only forms, so representative section-1-only syntax is now pinned to reject in ordinary boolean `expr` contexts. |
| 181 | `[1.4] iff gates event selection by boolean truth` | done | `ERP:iff_unknown_suppresses_event`, `ERP:iff_bounded_ops_and_literals`, `event_runtime_short_circuit_holds` | Verified: manifest coverage suppresses `x` and falsey guards at matching event timestamps, while the code-only short-circuit regression directly pins `1`/`0`/`x` guard outcomes on the same gated `posedge` surface. |
| 182 | `[2.2.1] Plain unsized decimal integers reject x and z` | done | `IBN:plain_decimal_literal_rejects_x_digit`, `IBN:plain_decimal_literal_rejects_z_digit`, `IBP:integral_literal_based_hex_unknown_x_digit`, `IBP:integral_literal_based_hex_unknown_z_digit` | Added direct `1x`/`1z` logical parse negatives and paired `'h1x`/`'h1z` positive literals so plain unsized decimal forms reject unknown digits while based literals with the same digit spellings remain accepted. |
| 183 | `[1.4][2.7] iff guard rejects event-expression-only syntax` | done | `ERN:iff_guard_rejects_event_wildcard`, `ERN:iff_guard_rejects_event_posedge`, `ERN:iff_guard_rejects_event_negedge`, `ERN:iff_guard_rejects_event_edge`, `ERN:iff_guard_rejects_event_or_union`, `ERN:iff_guard_rejects_event_comma_union`, `ERN:iff_guard_rejects_nested_event_iff`, `PP:iff_parenthesized_payload` | Added direct event-entrypoint negatives for wildcard, edge-keyword, union, and nested-`iff` guard spellings, while the existing parenthesized `||` parse-positive keeps section-2 boolean syntax accepted in the same guard slot. |
| 184 | `[1.4][2.4.1] iff guard enforces boolean-context operand restrictions` | done | `RTN:invalid_string_boolean_context`, `RTN:iff_guard_rejects_string_signal_boolean_context`, `RTN:iff_guard_rejects_string_literal_boolean_context`, `ERP:iff_bounded_ops_and_literals`, `RTP:rich_iff_with_triggered_real_and_string` | Added direct event-entrypoint negatives for string signal and string literal guards, so `iff` now proves the same boolean-context restriction it already enforces in ordinary logical expressions while keeping non-string boolean guards accepted across integral and rich-type surfaces. |
| 185 | `[1.2][1.6] Edge-event operand-reference resolution matrix` | done | `PP:posedge_operand_reference_resolution_matrix`, `PP:negedge_operand_reference_resolution_matrix`, `PP:edge_operand_reference_resolution_matrix`, `ERP:posedge_operand_reference_resolution_matrix`, `ERP:negedge_operand_reference_resolution_matrix`, `ERP:edge_operand_reference_resolution_matrix` | Added direct parse and runtime matrix fixtures for `posedge`, `negedge`, and `edge` over simple, hierarchical, and canonical operand references, so all documented edge-event name surfaces now resolve the same way as ordinary signal references. |
| 186 | `[1.4][1.5] Mixed union spellings remain semantically identical in multi-term chains` | done | `ERP:mixed_union_left_to_right_chain_or_then_comma`, `ERP:mixed_union_left_to_right_chain_comma_then_or`, `event_runtime_shadow_parity_matches_legacy_event_matches_for_non_iff_surface` | Added direct three-term runtime fixtures for both mixed-spelling orders with identical match sets, so multi-term `or`/`,` synonym behavior is now pinned beyond the legacy parity smoke test. |
| 187 | `[2.3.8][2.3.12] Recovered string-type casts obey string-only identity rules` | done | `RTP:operand_type_cast_string`, `RTN:invalid_recovered_string_cast_from_integral`, `RTN:invalid_recovered_integer_like_cast_from_string_literal` | Added direct recovered-target negatives for string-target/non-string-source and non-string-target/string-source spellings, so `type(ref)'(...)` now proves the same string-only identity rule as explicit `string'(...)`. |
| 188 | `[2.5.13] inside rejects non-integral lhs and set items` | done | `RTN:invalid_inside_lhs_real`, `RTN:invalid_inside_item_real`, `RTN:invalid_inside_range_lower_bound_real`, `RTN:invalid_inside_range_upper_bound_real` | Added direct rich-type negatives for a real lhs, a real singleton set item, and real lower/upper range bounds, so `inside` now pins integral-only typing across both lhs and bracketed set-item forms. |
| 189 | `[2.6] Bitwise OR binds tighter than logical AND` | done | `IBP:bitwise_or_logical_and_precedence_chain`, `IBP:bitwise_or_logical_and_matches_parenthesized_chain`, `IBP:bitwise_or_logical_and_alternate_grouping_has_distinct_result` | Added direct unparenthesized and parenthesized logical fixtures for `a | b && c`, plus the alternate `a | (b && c)` grouping with a distinct packed result, so the documented `|`-before-`&&` precedence is now pinned end to end. |
| 190 | `[2.7] Semantic validation enforces packed-only selection and operator type restrictions` | done | `IBN:invalid_selection_base_scalar_integer_like`, `RTN:invalid_bit_select_base_real`, `RTN:invalid_bit_select_base_string`, `RTN:invalid_bitwise_and_real_operands`, `RTN:invalid_comparison_string_operands` | Added direct grammar-valid semantic negatives for real/string selection bases plus exact `temp & temp` and `msg < msg` operator-family rejections, so the packed-only selection and operand-family guardrails are now pinned end to end. |
| 191 | `[2.7] Brace-form grammars reject empty and malformed concatenation, replication, and inside sets` | done | `IBN:empty_concatenation`, `IBN:malformed_inside_set`, `IBN:malformed_concatenation`, `IBN:malformed_replication_missing_inner_close`, `IBN:malformed_replication_missing_outer_close` | Added direct logical parse negatives for empty concatenation plus both missing-brace replication failure shapes, completing the brace-form rejection matrix alongside the existing empty `inside` set and malformed concatenation coverage. |
| 192 | `[1.4][2.1][2.2.2][2.4.1] iff guards reject raw events but accept .triggered` | done | `RTP:rich_iff_with_triggered_real_and_string`, `RTP:triggered_logical_primary`, `RTN:iff_guard_rejects_raw_event_value_use` | Rich-type fixtures now cover both sides of the contract: `.triggered` is accepted in `iff` guards, while a raw event guard is rejected before boolean conversion via the raw-event value-use guardrail. |
| 193 | `[2.2.2][2.7] .triggered rejects parenthesized raw-event forms` | done | `RTN:invalid_triggered_on_parenthesized_event` | Verified: the existing rich-type negative fixture exercises `(ev).triggered` and expects `EXPR-SEMANTIC-TRIGGERED`, so the parenthesized raw-event rejection is already covered. |
| 194 | `[2.2.2] Boolean-expression operand references must resolve to supported operands` | done | `IBN:unresolved_signal_boolean_primary`, `IBN:unresolved_signal_logical_and_operand`, `RTN:invalid_raw_event_boolean_primary`, `RTN:invalid_raw_event_logical_and_operand` | Added direct standalone and `&&` boolean-context negatives for both unknown identifiers and raw events, so section-2 operand references now reject unresolved names and non-value targets in both primary and embedded logical forms. |
| 195 | `[2.2.1] Unsupported unbased unsized integral literals are rejected` | done | `IBN:unbased_unsized_literal_rejects_zero`, `IBN:unbased_unsized_literal_rejects_one`, `IBN:unbased_unsized_literal_rejects_x`, `IBN:unbased_unsized_literal_rejects_z`, `IBP:integral_literal_based_decimal_unsigned_default`, `IBP:integral_literal_based_decimal_signed_default`, `IBP:integral_literal_based_hex_unknown_x_digit`, `IBP:integral_literal_based_hex_unknown_z_digit`, `IBP:integral_literal_sized_hex_unsigned_default`, `IBP:integral_literal_sized_hex_width_and_sign_default` | Added direct logical parse negatives for the unsupported SystemVerilog unbased unsized forms `'0`, `'1`, `'x`, and `'z`, while existing unsized and sized based-literal positives keep the documented supported literal surface accepted. |
| 196 | `[2.3.8][2.3.9] type(operand_reference) requires a resolvable operand reference` | done | `RTN:invalid_recovered_type_unknown_signal`, `RTN:invalid_enum_label_unknown_signal` | Added a direct enum-label unknown-source negative alongside the existing recovered-cast case, so both `type(operand_reference)'(...)` and `type(operand_reference)::LABEL` now prove unresolved operand references fail with the documented semantic diagnostic. |
| 197 | `[2.3.8][2.3.11] Recovered real-type casts obey ordinary real-cast x/z rejection` | done | `RTP:operand_type_cast_real`, `RTN:invalid_recovered_real_cast_from_unknown_bits`, `RTN:invalid_recovered_real_cast_from_z_bits` | Existing recovered-real positive coverage for `type(temp)'(count)` is now paired with direct `x`/`z` negatives, proving recovered real targets reuse the ordinary `real` cast rejection path. |
| 198 | `[2.5.14][2.7] Unsupported brace-based expression syntax is rejected` | done | `IBN:rejects_streaming_concatenation_left_to_right`, `IBN:rejects_streaming_concatenation_right_to_left`, `IBN:rejects_assignment_pattern_expression_primary` | Added direct logical parse negatives for both streaming-concatenation directions and standalone assignment-pattern-style `'{...}` syntax, so only documented concatenation and replication brace primaries remain accepted. |
| 199 | `[2.5.13][2.7] inside requires the exact braced set surface` | done | `IBN:inside_rejects_non_braced_rhs` | Added a direct logical parse negative for `a inside 4'd1`, so `inside` now rejects a non-braced RHS at the operator boundary instead of only malformed braced-set variants. |
| 200 | `[2.5.7][2.5.8] Arithmetic and exponentiation reject non-numeric operands` | done | `RTN:invalid_string_numeric_add`, `RTN:invalid_add_rhs_string`, `RTN:invalid_sub_lhs_string`, `RTN:invalid_mul_rhs_string`, `RTN:invalid_div_lhs_string`, `RTN:invalid_mod_rhs_string`, `RTN:invalid_unary_plus_operand_string`, `RTN:invalid_unary_minus_operand_string`, `RTN:invalid_power_lhs_string`, `RTN:invalid_power_rhs_string` | Direct rich-type negatives now cover string rejection across binary `+`, `-`, `*`, `/`, and `%`, unary `+`/`-`, and exponentiation in both operand orders. |
| 201 | `[2.6][2.7][2.5.3] Mixed postfix .triggered and selection chains obey left-to-right parsing and semantic restrictions` | done | `RTN:invalid_selection_on_triggered_scalar`, `RTN:invalid_triggered_after_raw_event_selection` | Direct rich-type negatives now pin both left-associated postfix orders: `ev.triggered[0]` reaches scalar-selection rejection, while `ev[0].triggered` surfaces the underlying raw-event selection failure before `.triggered` can apply. |
| 202 | `[2.5.3][2.5.14][2.7] Integer-only constant-expression positions reject constant non-integer operands` | done | `IBN:invalid_real_const_part_select_msb`, `IBN:invalid_real_const_part_select_lsb`, `IBN:invalid_string_const_part_select_msb`, `IBN:invalid_string_const_part_select_lsb`, `IBN:invalid_real_const_indexed_width`, `IBN:invalid_real_const_indexed_down_width`, `IBN:invalid_string_const_indexed_width`, `IBN:invalid_string_const_indexed_down_width`, `IBN:invalid_real_const_replication_multiplier`, `IBN:invalid_string_const_replication_multiplier` | Added direct real- and string-constant negatives for fixed part-select bounds, indexed part-select widths, and replication multipliers, proving integer-only `constant_expr` slots reject constant non-integer operands rather than only non-constant variables. |
| 203 | `[1.2][1.6] Event operand references use host-provided resolution` | done | `ERP:named_event_host_resolution_uses_host_context`, `ERN:unknown_signal_named_event` | The same source spelling `missing` resolves under an inline custom host fixture and fails under the baseline host profile. |
| 204 | `[2.3.8][2.3.13] Recovered raw event types are invalid cast targets` | done | `RTN:invalid_recovered_event_cast_target`, `RTP:triggered_int_cast_primary` | Added the missing `type(ev)'(1)` rich-type negative while keeping the existing `.triggered` cast positive as the contrast, proving recovered raw events stay invalid cast targets without blocking ordinary `.triggered` casts. |
| 206 | `[2.2.2][2.7] Non-whitelisted call-like primary syntax is rejected` | done | `IBN:rejects_non_whitelisted_call_like_empty_args`, `IBN:rejects_non_whitelisted_call_like_single_arg`, `IBN:rejects_non_whitelisted_call_like_hierarchical_empty_args` | Added direct logical parse negatives for `f()`, `f(a)`, and `top.fsm.next()`, while existing standalone and parenthesized positives continue to cover the whitelisted primary-expression surface. |
| 207 | `[1.1][1.6] Wildcard gated-event form * iff logical_expr` | done | `PP:wildcard_any_tracked_iff_gate`, `ERP:wildcard_any_tracked_iff_gate` | Added direct parse and event-runtime fixtures for `* iff gate`, including tracked-signal matches under guard values `1`, `0`, and `x` plus an untracked-only guard transition that does not trigger the wildcard term. |
| 208 | `[2.3.6][2.3.8][2.3.10] Recovered enum casts apply state-domain rules before label resolution` | done | `RTP:recovered_enum_cast_four_state_target_preserves_x_before_label_resolution`, `RTP:recovered_enum_cast_two_state_target_zeroes_x_before_label_resolution`, `RTP:recovered_enum_cast_four_state_target_preserves_z_before_label_resolution`, `RTP:recovered_enum_cast_two_state_target_zeroes_z_before_label_resolution` | Paired `x`/`z` fixtures now contrast recovered 4-state and 2-state enum targets: 4-state preserves unknown bits and stays unlabeled, while 2-state zeroes unknown bits first and then reacquires the matching enum label from the converted bit pattern. |
| 209 | `[2.5.3][2.7] Malformed selection postfix syntax is rejected` | done | `IBN:malformed_selection_empty_brackets`, `IBN:malformed_selection_missing_part_select_lsb`, `IBN:malformed_selection_missing_part_select_msb`, `IBN:malformed_selection_missing_indexed_up_base`, `IBN:malformed_selection_missing_indexed_down_base`, `IBN:malformed_selection_missing_indexed_up_width`, `IBN:malformed_selection_missing_indexed_down_width` | Added direct logical parse negatives for empty `[]`, missing part-select bounds, and both indexed part-select base/width failure shapes, while existing positive fixtures continue to cover the valid bit-, part-, and indexed-selection forms. |
| 210 | `[2.5.13][2.7] Malformed inside range-item syntax is rejected` | done | `IBN:malformed_inside_range_missing_upper_bound`, `IBN:malformed_inside_range_missing_lower_bound`, `IBN:malformed_inside_range_extra_colon`, `IBP:inside_range_includes_lower_bound`, `IBP:inside_range_includes_upper_bound` | Direct logical negatives now reject missing upper/lower bounds and an extra-colon range item, while the existing positive range fixtures keep the exact `[expr:expr]` form accepted. |
| 211 | `[1.6][2.2.2][2.7] Section-2 operand references use shared host resolution` | done | `IBP:operand_reference_host_resolution_uses_host_context`, `IBN:unresolved_signal_boolean_primary` | The same source spelling `missing` now resolves under an inline custom logical host fixture and fails under the baseline host profile, matching the shared host-resolution contract already covered on the event side. |
| 212 | `[1.2][1.6][2.2.2][2.7] Section-2 operand references accept canonical dump-derived signal tokens` | done | `IBP:canonical_token_operand_reference_resolution`, `IBP:canonical_token_operand_reference_resolution_prefers_exact_host_match` | Added direct logical-eval fixtures for `top.cpu.data[0]`, including an ambiguous host that also exposes `top.cpu.data`, proving section-2 operand references accept canonical dump-derived tokens and prefer the exact host-provided token match over a selection-shaped parse. |
| 213 | `[2.5.2][2.5.14] Concatenation operands stay self-determined before packing` | done | `IBP:concatenation_operands_keep_self_determined_literal_widths`, `IBP:concatenation_packs_each_item_after_its_own_self_determined_evaluation` | Added direct literal-width and nested-expression packing contrasts, proving concatenation contributes each operand's own self-determined width and bit pattern instead of re-coercing operands through a shared type before packing. |
| 214 | `[2.5.3] Indexed part-select base is evaluated at the current sample` | done | `IBP:indexed_up_part_select_base_uses_current_sample_initial`, `IBP:indexed_up_part_select_base_uses_current_sample_updated`, `IBP:indexed_down_part_select_base_uses_current_sample_initial`, `IBP:indexed_down_part_select_base_uses_current_sample_updated` | Direct paired `+:` and `-:` fixtures now reuse the same source with a time-varying base signal across two timestamps, proving indexed part-select base expressions are evaluated from the current sample instead of a cached or previous value. |

## Infra-Only / Harness-Only Coverage

These tests are important guardrails, but they do not map cleanly to a single
checklist point:

- `tests/expression_parse.rs::parse_no_panic_corpus_holds`: parser robustness
  against malformed event-input fuzz seeds.
- `tests/expression_fixture_contract.rs::*`: manifest schema, suite ownership,
  runtime-timestamp requirements, and snapshot hygiene.
- `src/expr/lexer.rs::lex_event_expr_tracks_keywords_and_spans`: lexer token/span
  regression coverage.
- `src/expr/parser.rs::typed_parser_rejects_unmatched_open_parenthesis` and
  `src/expr/parser.rs::typed_parser_rejects_broken_union_segmentation`: direct
  parser diagnostic regressions already reinforced by manifest negatives.
- `src/expr/eval.rs::logical_and_short_circuits_rhs`: evaluator-level
  short-circuit regression on a hand-built bound tree.
