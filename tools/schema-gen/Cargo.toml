[package]
name = "wavepeek-schema-gen"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
jsonschema = { version = "~0.46", default-features = false }
serde_json = "~1"
wavepeek = { path = "../.." }
